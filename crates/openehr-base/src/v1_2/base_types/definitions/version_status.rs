// @generated by openehr-codegen from BMM (`VERSION_STATUS`) — DO NOT EDIT.
// SPDX-FileCopyrightText: Ruben Talstra
// SPDX-FileCopyrightText: openEHR Foundation
// SPDX-License-Identifier: BUSL-1.1 AND Apache-2.0

//! The openEHR `VERSION_STATUS` spec class, generated from the vendored BMM
//! meta-model.

/// Status of a versioned artefact, as one of a number of possible values: uncontrolled, prerelease, release, build.
#[doc(alias = "VERSION_STATUS")]
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum VersionStatus {
    /// `alpha`
    Alpha,
    /// `beta`
    Beta,
    /// `release_candidate`
    ReleaseCandidate,
    /// `released`
    Released,
    /// `build`
    Build,
    /// A value outside the `VERSION_STATUS` constant set.
    ///
    /// NOTE: no openEHR spec governs an out-of-set value — our own
    /// tolerance-preserving design (`BMM_ENUMERATION` defines only the listed
    /// constants), retained so this enum's wire form stays byte-identical to
    /// the bare `String` it replaces.
    Other(String),
}

impl VersionStatus {
    /// The wire string of this constant (the verbatim token for
    /// [`Self::Other`]).
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Alpha => "alpha",
            Self::Beta => "beta",
            Self::ReleaseCandidate => "release_candidate",
            Self::Released => "released",
            Self::Build => "build",
            Self::Other(__s) => __s.as_str(),
        }
    }

    /// This constant for a wire string, tolerating an unknown token
    /// as [`Self::Other`] (total — never fails).
    #[must_use]
    pub fn from_wire(__s: &str) -> Self {
        match __s {
            "alpha" => Self::Alpha,
            "beta" => Self::Beta,
            "release_candidate" => Self::ReleaseCandidate,
            "released" => Self::Released,
            "build" => Self::Build,
            _ => Self::Other(__s.to_owned()),
        }
    }
}

impl ::core::convert::TryFrom<&str> for VersionStatus {
    type Error = UnknownVersionStatus;

    /// # Errors
    /// Returns [`UnknownVersionStatus`] when `__s` is not a `VERSION_STATUS` value
    /// (unlike [`Self::from_wire`], which is total).
    fn try_from(__s: &str) -> ::core::result::Result<Self, Self::Error> {
        match __s {
            "alpha" => ::core::result::Result::Ok(Self::Alpha),
            "beta" => ::core::result::Result::Ok(Self::Beta),
            "release_candidate" => ::core::result::Result::Ok(Self::ReleaseCandidate),
            "released" => ::core::result::Result::Ok(Self::Released),
            "build" => ::core::result::Result::Ok(Self::Build),
            _ => ::core::result::Result::Err(UnknownVersionStatus(__s.to_owned())),
        }
    }
}

/// The error returned by [`VersionStatus::try_from`] for a value outside the `VERSION_STATUS`
/// constant set.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UnknownVersionStatus(pub String);

impl ::core::fmt::Display for UnknownVersionStatus {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        ::core::write!(f, "unknown VERSION_STATUS value: {:?}", self.0)
    }
}

impl ::std::error::Error for UnknownVersionStatus {}
