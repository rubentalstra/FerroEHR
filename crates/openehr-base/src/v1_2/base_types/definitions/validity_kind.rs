// @generated by openehr-codegen from BMM (`VALIDITY_KIND`) — DO NOT EDIT.
// SPDX-FileCopyrightText: Ruben Talstra
// SPDX-FileCopyrightText: openEHR Foundation
// SPDX-License-Identifier: Apache-2.0

//! The openEHR `VALIDITY_KIND` spec class, generated from the vendored BMM
//! meta-model.

/// An enumeration of three values that may commonly occur in constraint models.
///
/// Use as the type of any attribute within this model, which expresses constraint on some attribute in a class in a reference model. For example to indicate validity
/// of Date/Time fields.
#[doc(alias = "VALIDITY_KIND")]
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub enum ValidityKind {
    /// `mandatory`
    Mandatory,
    /// `optional`
    Optional,
    /// `prohibited`
    Prohibited,
    /// A value outside the `VALIDITY_KIND` constant set.
    ///
    /// NOTE: no openEHR spec governs an out-of-set value — our own
    /// tolerance-preserving design (`BMM_ENUMERATION` defines only the listed
    /// constants), retained so this enum's wire form stays byte-identical to
    /// the bare `String` it replaces.
    Other(String),
}

impl ValidityKind {
    /// The wire string of this constant (the verbatim token for
    /// [`Self::Other`]).
    #[must_use]
    pub fn as_str(&self) -> &str {
        match self {
            Self::Mandatory => "mandatory",
            Self::Optional => "optional",
            Self::Prohibited => "prohibited",
            Self::Other(__s) => __s.as_str(),
        }
    }

    /// This constant for a wire string, tolerating an unknown token
    /// as [`Self::Other`] (total — never fails).
    #[must_use]
    pub fn from_wire(__s: &str) -> Self {
        match __s {
            "mandatory" => Self::Mandatory,
            "optional" => Self::Optional,
            "prohibited" => Self::Prohibited,
            _ => Self::Other(__s.to_owned()),
        }
    }
}

impl ::core::convert::TryFrom<&str> for ValidityKind {
    type Error = UnknownValidityKind;

    /// # Errors
    /// Returns [`UnknownValidityKind`] when `__s` is not a `VALIDITY_KIND` value
    /// (unlike [`Self::from_wire`], which is total).
    fn try_from(__s: &str) -> ::core::result::Result<Self, Self::Error> {
        match __s {
            "mandatory" => ::core::result::Result::Ok(Self::Mandatory),
            "optional" => ::core::result::Result::Ok(Self::Optional),
            "prohibited" => ::core::result::Result::Ok(Self::Prohibited),
            _ => ::core::result::Result::Err(UnknownValidityKind(__s.to_owned())),
        }
    }
}

/// The error returned by [`ValidityKind::try_from`] for a value outside the `VALIDITY_KIND`
/// constant set.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UnknownValidityKind(pub String);

impl ::core::fmt::Display for UnknownValidityKind {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        ::core::write!(f, "unknown VALIDITY_KIND value: {:?}", self.0)
    }
}

impl ::std::error::Error for UnknownValidityKind {}
