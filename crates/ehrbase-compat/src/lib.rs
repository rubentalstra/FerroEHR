//! `EHRbase`-compatible endpoint surface: `EhrScape` (`/rest/ecis/v1`) and the
//! FLAT / STRUCTURED / Web-Template JSON endpoints.
//!
//! Built in P17 as modern idiomatic Rust on the same service layer + `openehr-flat`
//! (ADR-006/008); the openEHR specs + Better `web-template` conventions are the
//! authority, `EHRbase` is prior art only. Empty scaffold until then.
