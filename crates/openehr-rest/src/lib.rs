//! openEHR ITS-REST 1.0.3 server surface (axum).
//!
//! Receives EHRbase's `rest-openehr` Java (moved here in P0); each `.java` is
//! ported to a `.rs` beside it from P6 onward, and the Java is deleted only
//! when its port reaches parity.
