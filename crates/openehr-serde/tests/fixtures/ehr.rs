//! Fixtures for the ehr package. Filled by the P4 fixture wave.

use super::Vector;

// TODO(port): P4 fixture wave — one deterministic fixture per concrete
// class in this package; the coverage test names every missing class.
pub fn fixtures() -> Vec<Vector> {
    Vec::new()
}
