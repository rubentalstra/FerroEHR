//! Content truth-table cases for master15/16/17 (`data_types`) — not yet transcribed.

use crate::registry::CaseEntry;

/// The implemented content (`data_types`) case entries (none yet).
#[must_use]
pub fn entries() -> Vec<CaseEntry> {
    Vec::new()
}
