//! master08 — CONTRIBUTION cases (design §4.1: `suites/contribution.rs`).
//!
//! Consumes `contributions/{valid,invalid}/{minimal,minimal_persistent}`
//! fixtures. Not yet transcribed.

use crate::registry::CaseEntry;

/// The implemented master08 case entries (none yet).
#[must_use]
pub fn entries() -> Vec<CaseEntry> {
    Vec::new()
}
