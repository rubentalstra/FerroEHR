//! master11 — QUERY (AQL) cases + `QUERY-FIXTURE-*` corpus (design §4.1, §3.4).
//!
//! master11 is a TBD stub upstream; the real query evidence is the vendored AQL
//! corpus (`query/aql_queries_{valid,invalid}` + `expected_results` golden sets),
//! run as `QUERY-FIXTURE-*` `FixtureDerived` cases. Not yet transcribed.

use crate::registry::CaseEntry;

/// The implemented master11 / QUERY-FIXTURE case entries (none yet).
#[must_use]
pub fn entries() -> Vec<CaseEntry> {
    Vec::new()
}
