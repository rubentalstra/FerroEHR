//! RM 1.1.0 `common.directory` package (plus `common.tags`' ITEM_TAG,
//! colocated here — see the provenance PORT NOTE in `item_tag.rs`).

pub mod folder;
pub mod item_tag;
pub mod versioned_folder;
