//! RM 1.1.0 `support` component (measurement + the terminology mapping
//! note; `support.terminology` itself is implemented in
//! `openehr-terminology` since P2).

pub mod external_environment_access;
pub mod measurement_service;
pub mod terminology;
