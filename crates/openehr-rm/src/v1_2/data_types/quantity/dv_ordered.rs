// @generated by openehr-codegen from BMM (`DV_ORDERED`) — DO NOT EDIT.
// SPDX-FileCopyrightText: Ruben Talstra
// SPDX-FileCopyrightText: openEHR Foundation
// SPDX-License-Identifier: BUSL-1.1 AND Apache-2.0

//! The openEHR `DV_ORDERED` spec class, generated from the vendored BMM
//! meta-model.

use crate::v1_2::data_types::quantity::date_time::dv_date::DvDate;
use crate::v1_2::data_types::quantity::date_time::dv_date_time::DvDateTime;
use crate::v1_2::data_types::quantity::date_time::dv_duration::DvDuration;
use crate::v1_2::data_types::quantity::date_time::dv_time::DvTime;
use crate::v1_2::data_types::quantity::dv_count::DvCount;
use crate::v1_2::data_types::quantity::dv_ordinal::DvOrdinal;
use crate::v1_2::data_types::quantity::dv_proportion::DvProportion;
use crate::v1_2::data_types::quantity::dv_quantity::DvQuantity;
use crate::v1_2::data_types::quantity::dv_scale::DvScale;

/// Closed subtype set of `DV_ORDERED`, dispatched on each payload's `_type`.
///
/// Abstract class defining the concept of ordered values, which includes ordinals as well as true quantities.
///
/// It defines the functions  `<` and `_is_strictly_comparable_to()_`, the latter of which must evaluate to `True` for instances being compared with the  `<` function, or used as limits in the `DV_INTERVAL<T>` class.
///
/// Data value types which are to be used as limits in the `DV_INTERVAL<T>` class must inherit from this class, and implement the function `_is_strictly_comparable_to()_` to ensure that instances compare meaningfully. For example, instances of `DV_QUANTITY` can only be compared if they measure the same kind of physical quantity.
#[doc(alias = "DV_ORDERED")]
#[derive(Debug, Clone, PartialEq)]
pub enum DvOrdered {
    /// The `DV_COUNT` subtype of `DV_ORDERED`.
    DvCount(DvCount),
    /// The `DV_DATE` subtype of `DV_ORDERED`.
    DvDate(DvDate),
    /// The `DV_DATE_TIME` subtype of `DV_ORDERED`.
    DvDateTime(DvDateTime),
    /// The `DV_DURATION` subtype of `DV_ORDERED`.
    DvDuration(DvDuration),
    /// The `DV_ORDINAL` subtype of `DV_ORDERED`.
    DvOrdinal(DvOrdinal),
    /// The `DV_PROPORTION` subtype of `DV_ORDERED`.
    DvProportion(DvProportion),
    /// The `DV_QUANTITY` subtype of `DV_ORDERED`.
    DvQuantity(DvQuantity),
    /// The `DV_SCALE` subtype of `DV_ORDERED`.
    DvScale(DvScale),
    /// The `DV_TIME` subtype of `DV_ORDERED`.
    DvTime(DvTime),
}
