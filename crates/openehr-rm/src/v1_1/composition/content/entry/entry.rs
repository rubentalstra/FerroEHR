// @generated by openehr-codegen from BMM (`ENTRY`) — DO NOT EDIT.
// SPDX-FileCopyrightText: FerroEHR contributors
// SPDX-FileCopyrightText: openEHR Foundation
// SPDX-License-Identifier: MIT AND Apache-2.0

//! The openEHR `ENTRY` spec class, generated from the vendored BMM
//! meta-model.

use crate::v1_1::composition::content::entry::action::Action;
use crate::v1_1::composition::content::entry::admin_entry::AdminEntry;
use crate::v1_1::composition::content::entry::evaluation::Evaluation;
use crate::v1_1::composition::content::entry::instruction::Instruction;
use crate::v1_1::composition::content::entry::observation::Observation;

/// Closed subtype set of `ENTRY`, dispatched on each payload's `_type`.
///
/// The abstract parent of all `ENTRY` subtypes.
///
/// An `ENTRY` is the root of a logical item of  hard  clinical information created in the  clinical statement  context, within a clinical session. There can be numerous such contexts in a clinical session. Observations and other Entry types only ever document information captured/created in the event documented by the enclosing Composition.
///
/// An `ENTRY` is also the minimal unit of information any query should return, since a whole `ENTRY` (including subparts) records spatial structure, timing information, and contextual information, as well as the subject and generator of the information.
#[doc(alias = "ENTRY")]
#[derive(Debug, Clone, PartialEq)]
pub enum Entry {
    /// The `ACTION` subtype of `ENTRY`.
    Action(Action),
    /// The `ADMIN_ENTRY` subtype of `ENTRY`.
    AdminEntry(AdminEntry),
    /// The `EVALUATION` subtype of `ENTRY`.
    Evaluation(Evaluation),
    /// The `INSTRUCTION` subtype of `ENTRY`.
    Instruction(Instruction),
    /// The `OBSERVATION` subtype of `ENTRY`.
    Observation(Observation),
}
