//! **ITS-BMM** — BMM serialization (the openEHR meta-model interchange format).
//!
//! The vendored BMM meta-model that drives code generation lives in
//! `openehr-codegen/vendor/bmm` (consumed by `openehr-codegen`'s own reader);
//! the generated runtime BMM/P_BMM object model is `openehr-lang`. This module
//! is a placeholder for any ITS-BMM concerns that belong on the serialization
//! surface rather than the generator or the object model.
