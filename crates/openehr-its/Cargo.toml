[package]
name = "openehr-its"
version = "0.1.0"
description = "openEHR ITS (Implementation Technology Specifications): canonical JSON + XML serialization, ITS-REST OpenAPI contract, ITS-BMM. Mirrors the specifications-ITS-{JSON,XML,REST,BMM} repos."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
serde.workspace = true
serde_json.workspace = true
quick-xml.workspace = true
thiserror.workspace = true

[dev-dependencies]
serde_json.workspace = true
# NOTE: the full fidelity gate (round-trip the corpus through generated
# `openehr-rm` types + ITS-JSON schema validation) will re-add
# `openehr-rm` and `jsonschema` here.

[lints]
workspace = true
