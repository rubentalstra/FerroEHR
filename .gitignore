# Rust
/target
**/*.rs.bk
*.pdb

# Cargo — remove the next line to commit Cargo.lock for reproducible bins
# (gitignore has no inline comments; the pattern must stand alone)
Cargo.lock

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Editor / OS
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store

# Claude Code local overrides
CLAUDE.local.md
.claude/settings.local.json
.claude/.session-start-head

# Coverage / bench artifacts
/coverage
lcov.info
*.profraw

# sccache
.sccache/
