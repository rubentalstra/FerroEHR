# Dark mode

Every console screen is themed twice, and the pictures below are the proof:
the same screens the rest of this chapter documents, captured with the topbar's
dark-mode toggle on. The choice persists per browser (`localStorage`), so it
survives a reload and follows you across screens; it is a display preference
and nothing else: no screen, control, or permission changes with it.

<!-- toc -->

## Dashboard

![The dashboard in dark mode: record counts, stored-query namespace tiles and
the commit-activity trend](img/dashboard/dashboard-dark.png)

## Templates

![The templates screen in dark mode: the operational-template listing with its
upload button](img/templates/templates-dark.png)

## Query Builder

![The point-and-click query builder in dark mode: template picker, criteria
rows and the generated AQL](img/queries/query-builder-dark.png)

## EHRs

![The EHR finder in dark mode: the subject lookup and the paged EHR
listing](img/ehrs/ehrs-dark.png)

![An EHR's compositions tab in dark mode: the row filters and the composition
listing](img/ehrs/compositions/list-dark.png)

## Demographics

![The demographics party browser in dark mode: the kind switcher, party lookup
and create card](img/demographics/demographics-dark.png)

## Terminology

![The terminology browser in dark mode: the selected terminology's descriptor
card and its lookups](img/terminology/terminology-dark.png)

## Operations

![The operations panel in dark mode: dependency health, build provenance and
the metric tiles](img/operations/operations-dark.png)

## System

![The system screen in dark mode: CDR status, the conformance manifest and the
served OpenAPI documents](img/system/system-dark.png)

## Audit log

![The audit browser in dark mode: the ATNA trail with its event-time, patient
and outcome filters](img/audit/audit-dark.png)

## Tenants

![The tenant registry in dark mode: the registered tenants and the tenant this
session resolves to](img/tenants/tenants-dark.png)

## Subscriptions

![The event subscriptions screen in dark mode: the subscription table and its
predicates](img/subscriptions/subscriptions-dark.png)

## FHIR connector

![The FHIR connector admin in dark mode: the mapping store and the
verification panels](img/fhir/fhir-dark.png)
