# Catalogue semantic audit — chapter CONTRIBUTION

- Chapter: CONTRIBUTION — CNF master08 (`I_EHR_CONTRIBUTION`)
- Date: 2026-07-24 (tracker issue #231, milestone v3.9.0)
- Case count: **32** (every YAML in `tools/cnf-runner/artifacts/schedule/contribution/`; no sampling)
- Verdict tally: **11 ok · 19 DEFECT · 2 AMBIGUITY** (defect families D1–D12 + minor M1–M2; ambiguity candidates A1–A2)
- Oracles read first-hand: CNF `master08-func_tc_ehr_contribution.adoc` (complete — all 23 case tables cell-by-cell, plus the data-set sections: `[[one_commit]]` matrix, multi-version combinations A–D, the EHR_STATUS reject/accept combinations, `[[folder_commit]]`); SM `docs/UML/classes/i_ehr_contribution.adoc` (all 5 operations + `.Errors`) and `update_version.adoc` (the `preceding_version_uid` attribute — decisive for D3); ITS-REST vendored OAS `ehr-codegen.openapi.yaml` (`contribution_create` 201/400/404/409 + `NewContribution`/`UpdateVersion` schemas, `contribution_get` 200/404, responses `201_CONTRIBUTION`/`400_CONTRIBUTION`/`200_CONTRIBUTION`/`404_CONTRIBUTION`/`404_unknown_ehr_id`/generic `409`/`422`); RM `common/master06-change_control_package.adoc` (§2.4 change types incl. the deletion procedure "data attribute is set to Void", §lifecycle states, §Deletion) + UML `org.openehr.rm.common.contribution.adoc` (versions "one or more"), `org.openehr.rm.common.original_version.adoc` (`data` 0..1), `org.openehr.rm.ehr.ehr.adoc` (`ehr_status` 1..1), `org.openehr.rm.ehr.ehr_status.adoc` (`subject` 1..1); TERM `crates/openehr-term/assets/en/openehr_terminology.xml` (audit change type 249/250/251/523; version lifecycle state 532/553/523); register `artifacts/registers/ambiguities.yaml` read in full (AMB-3/7/9/10/11/22/23/40 honored, not relitigated). Bindings: all 4 `bindings/its-rest/I_EHR_CONTRIBUTION.*` (+ the two `I_ADMIN_SERVICE.*contribution*` unrealized declarations) read against the OAS. Corpus: all 13 data-set/OPT keys resolved in `corpus/MANIFEST.yaml`, every fixture file present; OPT template ids extracted and matched against fixture template references (`minimal_event.opt` → `minimal_evaluation.en.v1` ✓; `minimal_persistent.opt` → `persistent_minimal.en.v1` ✓ — which is what exposes D1; `maximal.opt` → `test_all_types.en.v1` ✓). Capture chains traced through the runner (`exec/driver.rs` contribution envelope builder + `provision_commit_sets` + the `directory_version_uid` publication; `model/case.rs` requires model; `vocab.rs` `ChangeType` = CREATE/MODIFY/DELETED).

## Per-case verdicts

| case id | verdict | evidence — spec file+section read, what was checked | resolution |
|---|---|---|---|
| commit_contribution-valid_composition | DEFECT | master08 §commit_contribution-valid_composition cell-by-cell. Preconditions/flow ok (both OPTs provisioned; fixture rows minimal-event + maximal per the B.2 data sets); the wire captures resolve (binding `contribution_uid` from body `uid.value`, `version_uids` from `versions[*].id.value` — `201_CONTRIBUTION` returns the `Contribution` with OBJECT_VERSION_ID refs under Prefer representation). But the official post-condition cell "the version ID matches the `change_type` executed (creation = 1 …)" is not asserted: the case asserts only `change_type: CREATE`, while the assertion vocabulary carries `uid_pattern` (e.g. `"…::<system>::1"`) that encodes exactly this cell. The "new CONTRIBUTION" post-condition is a prose `assert: state` without a `verified_by` (the list cases show the convention). | D8 |
| commit_contribution-invalid_composition | ok | master08 §-invalid_composition; invalid fixture adjudicated in MANIFEST ("EVALUATION carries no data attribute", RM ehr EVALUATION.data 1..1 — verified); `validation_failed` → 400/422 per `400_CONTRIBUTION` + AMB-40 alt_status on the binding; post-condition count 0 = "nothing created". | none |
| commit_contribution-empty | ok | master08 §-empty ("no data in the CONTRIBUTION … error indicating the empty list"); RM common `contribution.adoc` description "one or more versions" makes the rejection derivable; `validation_failed` per `400_CONTRIBUTION` (syntactically invalid content) + AMB-40. | none |
| commit_contribution-valid_invalid_compositions | ok | master08 §-valid_invalid_compositions + the transaction NOTE ("the whole commit should behave like a transaction and fail") — the bundled two-member payload with ONE aggregate outcome and count-0 post-condition encodes it exactly. Minor: the two `${ds:…}` refs are not declared under `data_sets:` (the in-flow refs still validate via `ValueRef::DataSet` → `check_ds_ref`, so not load-bearing — consistency nit M2). | note M2 |
| commit_contribution-event_composition | DEFECT | master08 §-event_composition cell-by-cell: two commits, modification names the version from commit 1, change-type asserts + count 2 — all derivable (RM master06 §2.4). Defect is the ambiguity tag: `ambiguities: [AMB-3]` + the "ITS-REST: If-Match (AMB-3)" comment are misapplied — see D3. The official "same OBJECT_VERSION_ID with version number = 2" result cell rests on the count-2 read (uid_pattern would nail it — noted under D8's family, not defected here). | D3 |
| commit_contribution-persistent_composition | DEFECT | master08 §-persistent_composition; `cnf.opt.minimal_persistent` provisioned and both persistent fixtures reference `persistent_minimal.en.v1` (MANIFEST + OPT `<template_id>` verified). Same misapplied AMB-3 tag. | D3 |
| commit_contribution-delete | DEFECT + AMBIGUITY | master08 §-delete; RM master06 §2.4 ("deletion of existing item: a new ORIGINAL_VERSION whose data attribute is set to Void … change_type … `523\|deleted\|`") + §Deletion; TERM verified 523 in BOTH groups (AMB-11 tag correct); AMB-10 report-only post-condition correctly typed. Two findings: (a) the data-less delete member contradicts the OAS `UpdateVersion` schema, which lists `data` as REQUIRED — a genuine RM↔OAS self-contradiction with NO register entry (A1); (b) misapplied AMB-3 tag (D3). Official flow cell "The COMPOSITION should reference the same template_id" implies a data-carrying member — the RM Void-data reading is adopted without registration. | A1 + D3 |
| commit_contribution-two_commits_second_invalid | DEFECT | master08 §-two_commits_second_invalid; valid v1 then invalid modification; `validation_failed`; count 1. All derivable. Misapplied AMB-3 tag only. | D3 |
| commit_contribution-two_commits_second_creation | DEFECT | master08 §-two_commits_second_creation. **Fixture mismatch (blocking):** `requires.templates: [cnf.opt.minimal_event]` but both data sets are `cnf.composition.persistent.v1/.v2` whose MANIFEST/fixture `template_id` is `persistent_minimal.en.v1` — provided only by `cnf.opt.minimal_persistent`. Step 1 (`expect: created`) commits against an unprovisioned OPT and can only fail template-not-found (D1). Also: the header comment claims the rejection "is a versioning-state conflict (409 conflict)" while the case expects `validation_failed` (400) — self-contradictory prose (the 400 derivation via `400_CONTRIBUTION`'s "modification type does not match the operation" IS the sound one for a creation member naming a preceding version) — D7; ghost `REQUIREMENTS.md` reference; misapplied AMB-3 tag (D3). | D1 + D7 + D3 |
| commit_contribution-non_exiting_opt | DEFECT | master08 §-non_exiting_opt; expectation `template_not_found` (400 alt 422) is sound — `422.yaml` names "the underlying template is not known" verbatim, the binding maps the kind distinctly with AMB-40 alt_status, and the reserved-absent id `cnf.absent_template.v99` is adjudicated in MANIFEST (precondition "There are no OPTs loaded" realized in effect: the referenced id is absent regardless of shared-SUT OPTs). Defect: the header comment asserts the OPPOSITE of the artifact state ("the commit_contribution binding … does not distinctly map template_not_found, so the missing-OPT reject realizes as validation_failed") — stale and self-contradicting the `expect: template_not_found` line; two ghost `REQUIREMENTS.md` references. | D6 |
| commit_contribution-minimal_ehr_status | DEFECT | master08 §-minimal_ehr_status (D.1); default-status EHR (commits: none) matches precondition; change_type modification with the captured current status version (cross-interface `I_EHR_STATUS.get_ehr_status` capture `status_version_uid` verified in that binding); count 2. Two defects: official flow step 3 "Verify expected CONTRIBUTION uids and CONTRIBUTION count" is dropped — the uid half IS realizable on this ITS (capture `created.contribution_uid` + a `get_contribution` step; the count half is unrealized, AMB-22/AMB-33, and needs the tag if omitted) — D5; misapplied AMB-3 tag (the comment "the ITS-REST binding realizes the precedent as If-Match" is false for this wire) — D3. | D5 + D3 |
| commit_contribution-full_ehr_status | DEFECT | master08 §-full_ehr_status (D.2): official precondition 2 "The EHR contains a full EHR_STATUS (all the optional information is set, i.e. subject.external_ref etc)" is NOT realized — `requires.ehr` supports only `{commits: none\|any}` (`model/case.rs` EhrRequirement), so the EHR starts with the DEFAULT status and the full status is instead the committed payload (the comment "EHR with a full EHR_STATUS; mints ${ehr_id}" claims a state the provisioner cannot produce). The official title ("containing full EHR_STATUS") vs precondition muddle is real but unregistered — D4. Plus D5 (step 3 dropped, as D.1) and D3 (misapplied AMB-3). | D4 + D5 + D3 |
| commit_contribution-ehr_status_invalid_change_type | ok | master08 §-ehr_status_invalid_change_type (D.3) + reject case 1 ("change_type IN [creation, deleted] should be rejected, because the default EHR_STATUS was already created"); the creation row's `conflict` (409) derives from the OAS generic `409` text "a resource with same identifier(s) already exists" + the official "error indicating the EHR_STATUS already existing"; count-1 post-condition. The deleted row is the sibling split (structural, wire needs the preceding target — spec-cited in the sibling); the one-row matrix documents it. Official "create and delete" tokens normalized to TERM rubrics creation/deleted — sound. | none |
| commit_contribution-ehr_status_invalid_change_type_deleted | AMBIGUITY | Sibling split of D.3 (non-official id — the AMB-4 sibling precedent, here adjudicated in-file via `RM ehr (EHR class: ehr_status 1..1)`, multiplicity verified). Two underivable points: (a) the data-less deleted member (A1, as commit_contribution-delete); (b) `expect: conflict` — no OAS sentence assigns the cannot-delete-the-EHR_STATUS rejection to 409: the generic `409` text covers "already exists", not "cannot be deleted once created" (master08 reject case 1), and `contribution_create` enumerates no other candidate; the exact-code choice needs a register entry (the AMB-21/25/26 state-conflict-family precedent) — A2. No `ambiguities:` tag at all on the case. | A1 + A2 |
| commit_contribution-valid_directory | ok | master08 §-valid_directory (E.1): no-directory EHR (commits none, no `directory:` requirement), FOLDER creation accepted, CREATE assert; `cnf.folder.minimal` in MANIFEST, fixture present. | none |
| commit_contribution-fail_create_existing_directory | ok | master08 §-fail_create_existing_directory (E.2): directory provisioned via `requires.directory`; creation → `conflict` derives from the OAS generic `409` ("already exists") + the official "the root FOLDER already exists". | none |
| commit_contribution-fail_modify_non_existing_directory | ok | master08 §-fail_modify_non_existing_directory (E.3): no directory; random preceding uid per the official flow; `validation_failed` cites `400_CONTRIBUTION` verbatim ("the modification type does not match the operation - i.e. first version of a MODIFICATION") — quote verified. | none |
| commit_contribution-update_existing_directory | DEFECT | master08 §-update_existing_directory (E.4): directory provisioned; `${directory_version_uid}` IS published by the directory provisioning (`exec/driver.rs` 1598–1608; handle registered in `model/case.rs` when `requires.directory` is set) — capture chain sound; MODIFY assert + state post-condition. Defect: misapplied AMB-3 tag/comment. | D3 |
| get_contribution-existing | ok | master08 §get_contribution-existing (H.1): in-flow commit gives the known uid; GET 200 per `contribution_get` + `200_CONTRIBUTION`; `instance_of: CONTRIBUTION` on the canonical body. | none |
| get_contribution-empty_ehr | ok | master08 §-empty_ehr (H.2): commits-none EHR + fixed arbitrary uid; `not_found` per `404_CONTRIBUTION` ("…or when a CONTRIBUTION with contribution_uid does not exist"); SM error `contribution_does_not_exist`. | none |
| get_contribution-bad_ehr | ok | master08 §-bad_ehr (H.3): `server: empty` = "There are no EHRs on the server"; `not_found` per `404_CONTRIBUTION` (absent EHR branch); SM `ehr_id_does_not_exist`. | none |
| get_contribution-bad_contribution | DEFECT | master08 §-bad_contribution (H.4): expectation sound (`not_found`, fixed non-existing uid cannot collide with server-minted uids). Defect: precondition "The EHR has CONTRIBUTIONs" is declared via `commit: [cnf.composition.minimal_event.v1]`, but that corpus key is a SINGLE COMPOSITION OBJECT while `provision_commit_sets` (`exec/driver.rs` 951–998) provisions only ARRAY sets — `let Some(items) = set.as_array() else { continue }` silently commits NOTHING, so the official precondition is never established at run time (outcome unaffected, discriminating power lost). Dual attribution: catalogue (non-set key where the requires.commit vocabulary means bulk sets) + runner (silent skip instead of a hard error — R1). | D9 |
| has_contribution-existing | DEFECT | master08 §has_contribution-existing (G.1): "The result should be `true`" — a positive Boolean return — is realized as GET-200 because ITS-REST 1.1.0 has no existence probe. That realization is EXACTLY what register entry AMB-23 covers, yet no case and no binding carries a machine-readable AMB-23 reference anywhere in the repo (grep: zero hits); the binding comment instead cites a nonexistent `REQUIREMENTS.md` as "ambiguity candidate". Prose-only register reliance; the AMB-17/AMB-33 case-tag convention (verified in definition_adl14/admin chapters) is violated. Flow itself sound. | D10 |
| has_contribution-empty_ehr | DEFECT | master08 §-empty_ehr (G.2): "result should be `false`" → `not_found` via the same unregistered-at-artifact-level GET realization (AMB-23). Fixed uid on a commits-none EHR sound. | D10 |
| has_contribution-bad_ehr | DEFECT | master08 §-bad_ehr (G.3): the SM error path (`ehr_id_does_not_exist`) makes 404 directly derivable (`404_CONTRIBUTION`), but the case rides the same untagged AMB-23 realization. `server: empty` matches. | D10 |
| has_contribution-bad_contribution | DEFECT | master08 §-bad_contribution (G.4): "result should be `false`" → `not_found`; untagged AMB-23 realization (D10) + the same never-provisioned `commit:` precondition as get_contribution-bad_contribution (D9). | D10 + D9 |
| list_contributions-post_commit | DEFECT | master08 §list_contributions-post_commit (F.1); SM `list_contributions` verified 0..1 optional; the its-rest binding's `unrealized` block carries machine-readable `ambiguity: AMB-22` (correct), but the CASE cites AMB-22 in prose comments only — no `ambiguities: [AMB-22]` tag, violating the AMB-17/AMB-33 convention (D11). Binding comment also says the register entry is "proposed … documented in schedule/contribution/REQUIREMENTS.md" — stale (AMB-22 IS registered; the file does not exist). Latent: `commit:` single-object key (D9 class) once a future ITS realizes the wire. Post-condition delegates content verification via `verified_by: get_contribution-existing` (id exists ✓). | D11 |
| list_contributions-empty | DEFECT | master08 §-empty (F.2): positive empty list; commits-none EHR. Untagged AMB-22 (prose only). | D11 |
| list_contributions-non_existing_ehr | DEFECT | master08 §-non_existing_ehr (F.3): `server: empty`, fixed uid, `not_found` per SM error `ehr_does_not_exist`. Untagged AMB-22. | D11 |
| list_contributions-ehr_containing_ehr_status | DEFECT | master08 §-ehr_containing_ehr_status (F.4). Untagged AMB-22 (D11). Additionally the precondition "a CONTRIBUTION with a VERSION\<EHR_STATUS\>" is declared as `commit: [cnf.ehr_status.minimal]` — the commit provisioning drives every set through the `I_EHR_COMPOSITION.create_composition` binding (`exec/driver.rs` 961), which can never commit an EHR_STATUS (needs the status PUT or a contribution with modification + preceding) — a category error, latent today only because the binding is unrealized (D12). | D11 + D12 |
| list_contributions-ehr_containing_directory | DEFECT | master08 §-ehr_containing_directory (F.5): the `requires.directory` provisioning is the right construct for "a CONTRIBUTION with a VERSION\<FOLDER\>". Untagged AMB-22 only. | D11 |

## Defects

**D1 — two_commits_second_creation: wrong OPT provisioned (blocking).**
YAML: `requires.templates: [cnf.opt.minimal_event]` with `data_sets: [cnf.composition.persistent.v1, cnf.composition.persistent.v2]`. MANIFEST (`cnf.composition.persistent.v1/.v2`) and the fixtures themselves declare `template_id: persistent_minimal.en.v1`; that template lives only in `templates/minimal_persistent.opt` (`<template_id><value>persistent_minimal.en.v1</value>` verified) = corpus key `cnf.opt.minimal_persistent`. Step 1 expects `created` but commits against an unprovisioned OPT — on a conformant SUT the case can only fail with the missing-template rejection, misreporting the SUT. Official C.9 ("Commit two CONTRIBUTIONa on same COMPOSITION with second having change_type = creation" + the persistent-composition validity NOTE) requires the first commit to succeed. Fix: `templates: [cnf.opt.minimal_persistent]`.

**D3 — AMB-3 misapplied to the contribution wire (8 cases).**
Cases tagged `ambiguities: [AMB-3]` and/or commented "ITS-REST: If-Match (AMB-3)": `-delete`, `-event_composition`, `-persistent_composition`, `-two_commits_second_creation`, `-two_commits_second_invalid`, `-minimal_ehr_status`, `-full_ehr_status`, `-update_existing_directory`. AMB-3's ambiguity is "The SM does not say where preceding_version_uid lives for update … the ITS-REST binding realizes it as If-Match" — that is the COMPOSITION-update endpoint. For `commit_contribution` there is no silence and no If-Match: the SM `update_version.adoc` defines `preceding_version_uid: OBJECT_VERSION_ID` on UPDATE_VERSION itself ("Current version in service for which this version is an update"), the OAS `UpdateVersion` schema carries the same property, and the runner's envelope builder (`exec/driver.rs` 811–825) inserts it into the version member body; the `I_EHR_CONTRIBUTION.commit_contribution` binding sends only `Prefer`, never If-Match. Fix: drop the AMB-3 tags/comments on these 8 cases and cite `SM update_version.adoc` / OAS `UpdateVersion.preceding_version_uid` instead.

**D4 — full_ehr_status: official precondition not realized.**
Official D.2 precondition 2: "The EHR contains a full `EHR_STATUS` (all the optional information is set, i.e. `subject.external_ref` etc)". The case's `requires: ehr: { commits: none }` can only mint an EHR with the DEFAULT status (`model/case.rs`: `requires.ehr` is `none | { commits: none|any }`, nothing else), and the comment's claim "EHR with a full EHR_STATUS; mints ${ehr_id}" is unfulfillable. The catalogue instead commits the full status AS the tested modification — the official distinguishing axis (starting state full vs default) is lost, unregistered. Fix: establish the full status first (an explicit flow step committing `cnf.ehr_status.with_subject.alt` as modification, then the tested modification on top — the master08 scenario 3 "modifications already done" reading), or register the D.2 title-vs-precondition muddle as an editorial ambiguity and say which reading the catalogue takes.

**D5 — D.1/D.2 official verification step dropped (minimal_ehr_status, full_ehr_status).**
Official flow step 3 (both cases): "Verify expected `CONTRIBUTION` uids and `CONTRIBUTION` count for the EHR with `ehr_id`". Neither case captures `created.contribution_uid` nor performs any verification. The uid half is realizable on ITS-REST 1.1.0 (capture + `get_contribution`); the count half is unrealized (`contribution_count` — AMB-33; `list_contributions` — AMB-22) and its omission needs the machine-readable tag. Fix: add the capture + GET step; tag the count omission.

**D6 — non_exiting_opt: stale, self-contradicting comment + ghost references.**
The header comment states "the commit_contribution binding (unmodifiable) wires only 201/400/404/409 and does not distinctly map template_not_found, so the missing-OPT reject realizes as validation_failed" while the case expects `template_not_found` and the binding DOES map `template_not_found` distinctly (400, `alt_status: [422]`, AMB-40). Two references to a `REQUIREMENTS.md` that does not exist under `schedule/contribution/`. Fix: rewrite the comment to the current binding reality (kind distinguished by fixture, AMB-40/AMB-2 pattern); scrub the ghosts.

**D7 — two_commits_second_creation: self-contradicting rejection rationale.**
Header comment: "the rejected outcome is a versioning-state conflict (409 conflict) — … `already_exists` is not wired distinctly by the commit_contribution binding (see REQUIREMENTS.md)"; the case expects `validation_failed` (400) with the sound `400_CONTRIBUTION` mismatched-modification-type derivation on the expect line. One of the two rationales must go (the 400 one is derivable; 409 is not enumerated for this shape); scrub the ghost reference.

**D8 — valid_composition: official version-id cell unasserted.**
Official C.1 post-condition: "the version ID matches the `change_type` executed (creation = 1, modification/amendment = 2, 3, …)". The case asserts `change_type: CREATE` only; the assertion vocabulary's `uid_pattern` (`"…::<system>::1"` — used elsewhere in the catalogue) encodes the cell exactly. The "EHR has a new CONTRIBUTION" post-condition is prose `assert: state` with no `verified_by`. Fix: add `uid_pattern` to the version assert; add `verified_by: I_EHR_CONTRIBUTION.get_contribution-existing` (or an in-flow GET). Same-family note (not defected): C.5/C.6/C.7's "same OBJECT_VERSION_ID with version number = 2" cells rest on the count-2 versioned-object read; `uid_pattern: "…::<system>::2"` would pin them.

**D9 — `commit:` preconditions never provision (get/has bad_contribution; latent in list post_commit).**
`requires.commit: [cnf.composition.minimal_event.v1]` names a single-object corpus entry, but `provision_commit_sets` (`tools/cnf-runner/src/exec/driver.rs` 951–998) iterates only `set.as_array()` and SILENTLY skips non-arrays — the official precondition "The EHR has `CONTRIBUTIONs`" (G.4/H.4) is never established; the cases still pass because the probed uid is absent either way, but they no longer test the populated-EHR ground the schedule specifies. Attribution: catalogue (wrong key kind for the bulk-set vocabulary) + **runner machinery** (a skipped provisioning step must be a hard error, never silence). Fix: wrap the singles as one-element sets (or teach the provisioner single-object sets) AND make the runner fail loud on an unprovisionable `commit:` key.

**D10 — AMB-23 exists but is machine-referenced nowhere (4 has_* cases + binding).**
Register entry AMB-23 covers precisely the Boolean→GET-200/404 realization of `has_contribution`, yet no case carries `ambiguities: [AMB-23]` and the binding's comment cites the nonexistent `REQUIREMENTS.md` as an "ambiguity candidate" — stale (the entry is registered) and prose-only. The AMB-17/AMB-33 convention (unrealized/diverging realizations tagged at case level — verified in definition_adl14 and admin chapters) applies. Fix: `ambiguities: [AMB-23]` on `has_contribution-existing/-empty_ehr/-bad_ehr/-bad_contribution`; point the binding comment at AMB-23.

**D11 — AMB-22 untagged on the five list_contributions cases.**
The binding's `unrealized.ambiguity: AMB-22` is correct and machine-readable, but the five cases cite AMB-22 in comments only — no `ambiguities: [AMB-22]` tag (the AMB-17 delete_opt cases all carry the tag). The binding comment also calls AMB-22 "proposed" and points at `schedule/contribution/REQUIREMENTS.md` (nonexistent) — stale. Fix: tag the five cases; refresh the binding comment.

**D12 — list_contributions-ehr_containing_ehr_status: unprovisionable precondition (latent).**
`commit: [cnf.ehr_status.minimal]` — the commit provisioning path drives every set through the `I_EHR_COMPOSITION.create_composition` binding (`exec/driver.rs` 961), which cannot commit an EHR_STATUS (the status wire is the PUT with a preceding target, or a contribution with `change_type: modification`). Harmless today (the case is N/A under AMB-22) but wrong the day a later ITS realizes the wire. Fix: an explicit flow/provisioning step through the status modification path (the minimal_ehr_status pattern).

**A1 — AMBIGUITY (register entry needed): data-less delete member — RM vs OAS.**
RM `common/master06-change_control_package.adoc` §2.4: "deletion of existing item: a new `ORIGINAL_VERSION` whose data attribute is set to Void … `change_type` is set to the code `523|deleted|`"; `original_version.adoc`: `data` 0..1. The OAS `UpdateVersion` schema: `required: [lifecycle_state, data, commit_audit]` — a delete member WITHOUT `data` violates the wire schema the SUT may enforce, and the official C.7 flow cell ("The `COMPOSITION` should reference the same `template_id`") reads as a data-carrying member. The catalogue (cases `-delete`, `-ehr_status_invalid_change_type_deleted`; runner envelope builder `exec/driver.rs` 776–808) adopts the RM Void-data reading with no register entry. Needs an `ambiguities.yaml` entry (typed disposition, both citations quoted) + tags on the two cases; SPECPR candidate (relax `UpdateVersion.required` for the deletion case or define the delete-member wire shape).

**A2 — AMBIGUITY (register entry needed): status code for the cannot-delete-EHR_STATUS rejection.**
master08 reject case 1: the EHR_STATUS "can't be deleted once created". `contribution_create` enumerates 201/400/404/409; the generic `409` text covers only "a resource with same identifier(s) already exists" — no sentence assigns the cannot-delete state-conflict, and `400_CONTRIBUTION` scopes parse/mismatch failures. The case asserts `conflict` (409) exactly, underivably. Needs an AMB-21/25/26-style entry (state-conflict family → 409, flag for upstream code assignment) + the tag on `-ehr_status_invalid_change_type_deleted`.

**M1 (minor) — MANIFEST consistency:** `cnf.opt.minimal_persistent` lacks the `template_id:` field every other OPT entry carries (`cnf.opt.minimal_event`, `cnf.opt.maximal` have it).
**M2 (minor) —** `valid_invalid_compositions` uses two `${ds:…}` refs without declaring them under `data_sets:` (refs still manifest-checked via the reference grammar; declaration consistency only).

## REQUIREMENTS.md references (known systemic issue — ghost file; enumeration in scope)

The file `tools/cnf-runner/artifacts/schedule/contribution/REQUIREMENTS.md` does not exist. References:

1. `schedule/contribution/I_EHR_CONTRIBUTION.commit_contribution-non_exiting_opt.yaml` — line 7 ("See REQUIREMENTS.md (existing-binding extension need)") and line 33 ("see NOTE / REQUIREMENTS.md")
2. `schedule/contribution/I_EHR_CONTRIBUTION.commit_contribution-two_commits_second_creation.yaml` — line 7 ("see REQUIREMENTS.md")
3. `bindings/its-rest/I_EHR_CONTRIBUTION.has_contribution.yaml` — line 8 ("See REQUIREMENTS.md (ambiguity candidate)" — the candidate IS registered as AMB-23)
4. `bindings/its-rest/I_EHR_CONTRIBUTION.list_contributions.yaml` — line 10 ("The proposed register entry (AMB-22) is documented in schedule/contribution/REQUIREMENTS.md" — AMB-22 IS registered)

## Coverage vs the official chapter (master08)

Every official Test Case row has a catalogue counterpart; one extra catalogue case is the registered-in-prose sibling split:

| official row | catalogue case |
|---|---|
| commit_contribution-valid_composition (C.1) | ✓ same id |
| commit_contribution-invalid_composition (C.2) | ✓ |
| commit_contribution-empty (C.3) | ✓ |
| commit_contribution-valid_invalid_compositions (C.4) | ✓ |
| commit_contribution-event_composition (C.5) | ✓ |
| commit_contribution-persistent_composition (C.6) | ✓ |
| commit_contribution-delete (C.7) | ✓ |
| commit_contribution-two_commits_second_invalid (C.8) | ✓ |
| commit_contribution-two_commits_second_creation (C.9) | ✓ (D1) |
| commit_contribution-non_exiting_opt (C.10) | ✓ |
| commit_contribution-minimal_ehr_status (D.1) | ✓ |
| commit_contribution-full_ehr_status (D.2) | ✓ (D4) |
| commit_contribution-ehr_status_invalid_change_type (D.3) | ✓ + sibling `-ehr_status_invalid_change_type_deleted` (split, non-official id) |
| commit_contribution-invalid_ehr_status (D.4) | ✓ |
| commit_contribution-valid_directory (E.1) | ✓ |
| commit_contribution-fail_create_existing_directory (E.2) | ✓ |
| commit_contribution-fail_modify_non_existing_directory (E.3) | ✓ |
| commit_contribution-update_existing_directory (E.4) | ✓ |
| list_contributions-post_commit (F.1) | ✓ (N/A per AMB-22) |
| list_contributions-empty (F.2) | ✓ (") |
| list_contributions-non_existing_ehr (F.3) | ✓ (") |
| list_contributions-ehr_containing_ehr_status (F.4) | ✓ (") |
| list_contributions-ehr_containing_directory (F.5) | ✓ (") |
| has_contribution-existing (G.1) | ✓ |
| has_contribution-empty_ehr (G.2) | ✓ |
| has_contribution-bad_ehr (G.3) | ✓ |
| has_contribution-bad_contribution (G.4) | ✓ |
| get_contribution-existing (H.1) | ✓ |
| get_contribution-empty_ehr (H.2) | ✓ |
| get_contribution-bad_ehr (H.3) | ✓ |
| get_contribution-bad_contribution (H.4) | ✓ |

The SM operation `I_EHR_CONTRIBUTION.contribution_count` (1..1 in `i_ehr_contribution.adoc`) has no master08 Test Case section and no EHR_CONTRIBUTION-chapter catalogue case (the admin-side `I_ADMIN_SERVICE.contribution_count` unrealized binding exists under AMB-33) — an official-schedule gap worth an upstream note, not a catalogue defect.

**Official data-set / combination tables with NO catalogue counterpart** (master08 defines them as data-set sections rather than Test Case rows, but the chapter text says they "should be tested"):

- The `[[one_commit]]` single-version matrix (12 rows): first-commit `amendment`/`modification`/`deleted` with valid content → rejected; `creation` with `lifecycle_state = deleted` → rejected. No catalogue case commits a first-commit non-creation change type for a COMPOSITION (the directory E.3 case is the only nothing-to-modify probe).
- Multi-version ACCEPTED combinations A/B/C ("Creating two valid … `COMPOSITIONs` in one commit should be accepted": two events, two persistents on different OPTs, mixed categories). **No case in the chapter commits a multi-VERSION contribution expecting acceptance** — the only bundled-payload case is the rejection (C.4). This is the change-set capability's defining positive and is untested.
- Combination D's remaining rows (valid+invalid persistent/persistent, event/persistent, invalid-first orderings) — only the event/event ordering of D is encoded (C.4).
- The EHR_STATUS accepted-combination matrix (`is_modifiable` × `is_queryable` × `subject.external_ref` — 12 unique rows) plus the archetype_id-variation NOTE: D.1/D.2 exercise one combination each (`PARTY_SELF` minimal; `external_ref` set).
- The EHR_STATUS reject case `lifecycle_state = incomplete` (master08 reject case 2, SPECPR-368 / AMB-9 report-only): no catalogue case commits an incomplete-lifecycle EHR_STATUS.
- The `[[folder_commit]]` row `deleted | deleted | valid | accepted` (directory deletion via contribution): E.1–E.4 cover creation/wrong-creation/wrong-modification/modification only.
- B.2 valid-COMPOSITION variants beyond minimal-event + maximal: time-series COMPOSITION, alternative-types, `DV_CODED_TEXT` on a `DV_TEXT` node, empty `ELEMENT.value` with `null_flavour` (the maximal set covers "all ENTRY types" in one instance, so B.2.a's per-ENTRY minimal rows are covered in aggregate only).
