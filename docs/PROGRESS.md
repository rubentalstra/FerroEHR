# Progress

One row per phase, updated when a phase's exit criteria are met (see
`docs/plans/current-phase.md` for the live pointer and the per-phase files
under `docs/plans/` for detail). Status values: `not-started`, `in-progress`,
`blocked`, `done`.

> **⚠️ ADR-004 pivot (2026-07-03):** the openEHR spec crates are now **generated
> from BMM** by `openehr-codegen`, not hand-transcribed. The P1/P3/P4 "done" rows
> below describe the superseded hand-transcription; that code has been
> regenerated. `openehr-base`, `openehr-rm`, and `openehr-am` (both AM 1.4 + 2.4)
> are now generated and compile clean. See the 0G rows and
> `docs/ADRs/ADR-004-spec-driven-codegen.md`.

| Phase | Title | Status | Note |
|---|---|---|---|
| 0R | Crate rename/consolidation | done | `openehr-*` (spec) / `ehrbase-*` (app) split; foundation→base, terminology→term, odin+bmm→lang, adl→am, aql→query, rest/compat/server→ehrbase-*. |
| 0G | Spec-driven codegen (ADR-004) | in-progress | `openehr-lang` (ODIN+BMM reader) + `openehr-codegen` (emitter) + `openehr-derive` (OpenEhrType) built; `openehr-base`/`openehr-rm`/`openehr-am` generated & compiling clean; cross-crate refs resolve to `openehr_base::prelude`. Left: fidelity-gate re-wire, `codegen.toml`, canonical XML, drift CI. |
| P0 | Scaffolding | done | 13-crate workspace green (build/fmt/clippy/deny); 428 Java files relocated per §9.1; harness + hooks live; reference/v1 = v0.32.0; archaeology recorded. Research dossier texts still to be committed. |
| P1 | Foundation + Identification (BASE) | superseded | Hand-transcription regenerated by codegen (ADR-004): BASE 1.3.0 now generated → `openehr-base` (foundation folded in). |
| P2 | Terminology bundle (TERM 3.x) + service API | done | `openehr-term` stays hand-written (bundle/assets/logic not in BMM): TERM assets vendored, rm.support service surface, id=532 regression test. |
| P3 | RM transcription | superseded | Hand-transcription regenerated by codegen (ADR-004): RM 1.2.0 now generated → `openehr-rm` (171 files, compiles clean). |
| P4 | Canonical JSON serialization (ITS-JSON) | superseded | `_type` self-tagging moved from the hand-written `TypeTag`/`serde_support` to the `#[derive(OpenEhrType)]` macro (openehr-derive). The full-RM fidelity harness needs re-wiring against the generated types (corpus preserved in `openehr-its/tests/vendor/`). |
| P5 | Canonical XML serialization (ITS-XML) | not-started | Hand-written in `openehr-its` via `quick-xml`; the one wire format the OpenEhrType derive does not cover. |
| P6 | REST skeleton (axum) | not-started | Every ITS-REST 1.0.3 endpoint + admin + item tags. |
| P7 | Persistence schema | not-started | Flyway migrations copied verbatim; sea-query tables; testcontainers PG18. |
| P8 | ODIN + BMM parsers | in-progress | Pulled forward by ADR-004: `openehr-lang` has a working ODIN reader + BMM JSON loader feeding codegen. Full ODIN grammar for ADL instances still to come. |
| P9 | ADL 1.4 + AOM 1.4 + OPT 1.4 XML | in-progress | AM object model generated → `openehr-am` (`am14` + `am24`). ADL/AOM *parsers* + OPT XML ingestion still to come. |
| P10 | WebTemplate builder | not-started | OptVisitor equivalent. |
| P11 | Composition validation | not-started | ValidationWalker equivalent + terminology binding. |
| P12 | AQL parser + AST + semantic path analysis | not-started | |
| P13 | AQL engine: AST → ASL → SQL | not-started | |
| P14 | rm-db-format bridge | not-started | RM ↔ decomposed row-per-locatable. |
| P15 | Service layer | not-started | Orchestration, transactions, versioning, contributions, audit. |
| P16 | FLAT / STRUCTURED / Web Template + EhrScape | not-started | |
| P17 | Make it compile | not-started | Drive `cargo check` to zero across the workspace, leaf-first. |
| P18 | Test parity | not-started | Target ≥99% parity on Linux x86_64 first. |
| P19 | Optimization | not-started | PG18 AIO tuning, hot-read pipelining, JSON_TABLE codegen. |
| P99 | Cutover | not-started | Delete remaining ported-out Java and residual Maven config; tag first pure-Rust release. |
