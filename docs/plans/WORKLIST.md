# Worklist — the single open-items tracker

Owner-requested (2026-07-12): every open work item lives HERE, one row each,
with status + the governing plan/branch. **This file is the single entry
point** (the former `current-phase.md` pointer was retired 2026-07-17);
close a row by linking the merged PR, and delete the row's plan file in the
PR that implements it. Never track work only in chat.
Fully refreshed 2026-07-16 (post platform-rewrite; stale rows pruned to the
closed table at the bottom).

## Open

| #       | Item                                                                                                                                                                                                                                                                                                                                                                                         | Status              | Where                                                             |
|---------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------|-------------------------------------------------------------------|
| EXAMPLE | **Issue #94 — template example generator emits only the skeleton** (registered as F-44): rewrite the example generator to walk the full WebTemplate tree and synthesize spec-valid values for every field (multi-archetype templates covered); the generated example must pass our own composition validation. Proper scoping first (generator design vs the WebTemplate builder), then fix. | queued              | [issue #94](https://github.com/rubentalstra/ehrbase-rs/issues/94) |
| SCRUB   | **Tracker-ID comment scrub** (owner hard rule 2026-07-16: no F-nn/S-nn/G-nn/W-nn/wave/phase markers anywhere in code — only `docs/specs/openehr/` citations): ~700 occurrences across ~170 files; partial slice landed; the six scrub agents relaunch when the session limit resets (file lists staged in the session scratchpad).                                                           | parked (rate limit) | this worklist                                                     |
| UI-2    | **Admin console — design overhaul + 100% feature completeness** (owner 2026-07-17: "we need an full 100% compleet admin UI"): a real visual design system (layout, typography, spacing, color, dark mode, empty/loading/error states) replacing the bare default-widget look, plus the missing features — query result export, EHR creation, composition commit/update from the UI, template + stored-query admin actions, builder order-by/column picker, table pagination, tab-state-in-URL, action feedback (toasts). Screen inventory in flight; screenshots re-captured per the ui-screenshot-guard.                                                                                                                                                             | in progress         | branch `claude/admin-ui-design-overhaul` (after the seeded-journeys branch merges) |
| ADL2    | **ADL2 — full implementation, spec-exact** (owner 2026-07-19: STARTS in the next Fable 5 session — "we need and we will support it soon"): ADL2/cADL2/ODIN source parser, the complete AOM2 semantic-validation catalogue, specialisation flattening, OPT2, template semantics. Oracle: `docs/specs/openehr/AM/docs/{ADL2,AOM2,OPT2}/` (+ the vendored AM 2.4 BMM already generated as `openehr-am::am24`). **Prior-art directive (owner 2026-07-19): openEHR's `archie` (the openEHR Java reference RM/AM library, github.com/openEHR/archie) ALREADY implements ADL2 end-to-end** — an ANTLR `AdlParser` for ADL2/cADL/ODIN, the AOM2 object model, archetype validation, the flattener/`OperationalTemplate` (OPT2) creator — **and ships a working ADL 1.4 → ADL 2 migration path** (its `ADL14ConversionUtil`/adl14 converter module: converts 1.4 archetypes/OPTs to ADL2, incl. term-binding/at-code conversion quirks). The next session MUST study archie's design first-hand (parser structure, AOM2 validation catalogue, flattening rules, and especially the 1.4→2 conversion strategy — we already store ADL 1.4 OPTs, so an in-CDR 1.4→2 migration/upgrade story matters) as PRIOR ART informing our own idiomatic Rust design — the vendored specs stay the oracle (spec-adherence.md), archie is never a parity target. The current REST seam already serves ADL2 artefacts (upload/list/get as text/plain source with 409/422 semantics; `_example_get`/`_version_get` return 501 pending this row — see `api/definition/template_adl2.rs` + the enriched OpenAPI declarations from the OAPI row). | in progress (2026-07-19: A1+A2 done; A3a done — cADL definition parser → generated am24 CComplexObject (306/319 corpus definitions parse, 27 S-codes raised, 42 tests green); corpus INVENTORY.md landed (regression-tag oracle, 13 filename mismatches, 85-code hand-written-case gap list); LANG placements settled: ODIN shipped, BEL→`openehr_lang::bel` at A3b, EL not needed; A3b done — codegen cross-schema re-emission (AM-level typed beom closure per the generator-first hard rules, ~75 classes re-emitted, LANG byte-identical, declare_hand_written_modules defect fixed), `openehr_lang::bel` parser over a generic BelBuilder seam, `openehr-adl` rules + real slot-assertion trees (88/88 incl. its fidelity gates); next: A3c ODIN→typed AOM assembly + printer + round-trip) | branch `feat/adl2` (renamed from `claude/adl2` per the 2026-07-19 branch-naming hard rule); plan `docs/plans/adl2.md` |
| CODEGEN | **`openehr-codegen` + `openehr-derive` — full redesign + rewrite of the generation subsystem** (owner 2026-07-19, derive added same day: it predates the first product release and no longer reflects the current design ideas): the generator has grown by accretion into oversized single files (`emit.rs` carries BMM→Rust type emission, abstract-enum/_type dispatch, cross-schema re-emission, back-reference/cycle handling; `main.rs` carries schema assembly + module declaration) and needs a ground-up redesign: a proper pipeline architecture (schema loading/merging → model analysis [closures, cycles, polymorphic seams, back-refs] → emission planning → per-crate rendering), one module per concern, the override/decision maps as declarative data with spec citations, and the emitter invariants (completeness — never minimize; source-package mirroring; byte-determinism; LANG/AM/RM parity) encoded as tested properties. **Scoping decision in-row: move the codegen crate to `tools/openehr-codegen`** (it is dev tooling, not an openEHR spec crate — nothing ships it; `openehr-derive` STAYS in `crates/` as a real proc-macro dependency of the spec crates). **`openehr-derive` redesign constraints:** it defines the canonical-JSON `_type` wire behaviour for the whole spec layer, so the rewrite is architecture-only at cutover — wire behaviour FROZEN, proven by the openehr-its fidelity gates + ITS-JSON schema oracle + a zero-drift ECC run; any deliberate tolerance-rule change is its own later, spec-cited, ECC-gated change, never mixed into the rewrite. Redesign together with the emitter (one generation subsystem: emitter decides the types, derive defines their wire discipline — the seam between them gets designed, not inherited). Hard constraints: output byte-identical at cutover (the codegen-drift gate is the proof), all three codegen hard rules preserved, `/regen-codegen` + CI wiring updated in the same PR. **Sequenced AFTER the ADL2 row closes** — ADL2 phases are actively extending this emitter; rewriting under them would collide. | queued (after ADL2) | plan to be authored at row start |
| UI-3    | **Admin console — full as-shipped review + spec-compliance audit + the next plan** (owner 2026-07-18): after develop is green and v3.2.0 is released, a fresh Fable 5 session walks the COMPLETE shipped admin-ui and its plan (`docs/plans/admin-ui-overhaul.md` — kept, NOT deleted, per owner), audits every surface the console touches against the official specs (openEHR ITS-REST/RM + IHE/FHIR where relevant), and authors a new plan file for the next iteration of the best possible spec-following admin UI. | queued (after v3.2.0) | plan to be authored by the follow-up session                       |

## Closed (recent; full history in `docs/PROGRESS.md` + git)

| #                                     | Item                                                                                                                                                                                                                                                                                                                                                            | Closed                                                                                                                                                                                                                     |
|---------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| OAPI                                  | Served-OpenAPI completeness sweep — all 162 utoipa declarations spec-exact against BOTH ITS-REST halves (op YAMLs + the overview prose), reconciliations recorded per module, plus the owner-directed wire fixes (disabled admin → the OAS-declared 405, tags updates honour Prefer with the spec-default 204, demographic Last-Modified, relationship return=identifier, ehr_create_with_id 200→201 correction), the structural completeness gate test, and a tracker-ID/internal-citation scrub of touched files; workspace 2002/2002, ECC 386/351/0 zero-drift                                                                                                             | [PR #129](https://github.com/rubentalstra/ehrbase-rs/pull/129), 2026-07-19                                                                                                                                 |
| DIR                                   | DIRECTORY full spec audit + total rewrite — the five ITS-REST operations audited first-hand (op YAMLs + overview prose + RM master05 + CNF master09) with the real gaps closed (`?path=` on the version read, ETag+Last-Modified on directory reads, create-after-delete opens a new hierarchy via the live-slot probe, complete utoipa declarations), 21-test spec-cited wire battery, and the console Directory tab rebuilt into a complete experience (structured tree editor, version history + restore, at-time, path query, delete+recreate) with 3 new e2e journeys; en route: wasm disposal panics + hydration-wiped finder inputs root-caused and fixed (owner: pre-existing defects fixed properly too); workspace 1998/1998, battery 16/16, ECC 386/351/0 zero-drift                                                                                                             | [PR #128](https://github.com/rubentalstra/ehrbase-rs/pull/128), 2026-07-19                                                                                                                                 |
| TESTPERF                              | Testing-framework full redesign + rewrite — the shared `tools/testkit` harness (one PG18 server + fingerprinted template database + clone per test) replaces per-test containers across all 50 DB test files; measured 791 s → 329 s full-workspace wall (2.4×), the 300 s route-coverage offender → 5.0 s, 1958/1958 green, coverage unchanged; CI runs zero docker-in-docker test containers; ECC 386/351/0 zero-drift                                                                                                                                                                                             | [PR #127](https://github.com/rubentalstra/ehrbase-rs/pull/127), 2026-07-18                                                                                                                                 |
| ATNA                                  | ATNA audit-trail full redesign (dual DICOM+BALP format with the ITI-20 MSGID/EventID conformance fixes; local Audit Record Repository ON by default in the new `audit` schema; ITI-20 ATX:FHIR Feed with outbox durability; ITI-81 `GET /fhir/r4/AuditEvent` admin retrieval; ITI-19 native mTLS via `[server.tls]`; `[atna]`→`[audit]` config redesign; dedicated book chapter; plan file deleted per the lifecycle — the admin-console audit-log browser landed on the design kit with e2e journeys + book screenshots; ECC deferred by owner to the joint ATNA+UI-2 convergence run)                                                                | branch `claude/atna-audit-rewrite`, 2026-07-18                                                                                                                                                                             |
| W-21                                  | Release v3.1.0 + v3.1.1 (minor bump: Simplified Formats rewrite + governance overhauls); en route: the tenant-GUC pool-stamp defect fixed with a deterministic regression test (#110), the release.yml binary-asset build fixed after the five-workflow audit (v3.1.0 published notes-only; v3.1.1 carries the tarball assets, prerelease until owner sign-off) | [PR #109](https://github.com/rubentalstra/ehrbase-rs/pull/109) / [#110](https://github.com/rubentalstra/ehrbase-rs/pull/110) / [#111](https://github.com/rubentalstra/ehrbase-rs/pull/111), tags v3.1.0+v3.1.1, 2026-07-17 |
| ADMIN-UI                              | The Leptos admin console shipped end-to-end (see `docs/PROGRESS.md` ADMIN-UI row: full screen set, the AST-emitting Query Builder, the Rust-native browser E2E battery green 7/7 with 7 real defects found+fixed pre-merge, third OCI image + compose + book chapter with harness-captured screenshots, ECC zero-drift)                                                                    | branch `claude/admin-ui`, 2026-07-18                                                                                                                                                                                       |
| FLAT                                  | Simplified Formats spec-exact greenfield rewrite (openehr-flat + REST negotiation from the STABLE ITS-REST spec; ECC-SF area added; plan file deleted per the lifecycle)                                                                                                                                                                                        | [PR #108](https://github.com/rubentalstra/ehrbase-rs/pull/108), 2026-07-17                                                                                                                                                 |
| W-16                                  | Issue #95 — format support via Accept header (subsumed by the FLAT rewrite: strict RFC 9110 negotiation on every relevant endpoint, 406/415 correctness, book docs updated)                                                                                                                                                                                     | [PR #108](https://github.com/rubentalstra/ehrbase-rs/pull/108), 2026-07-17                                                                                                                                                 |
| W-13                                  | Configuration redesign — one TOML file                                                                                                                                                                                                                                                                                                                          | merged [PR #96](https://github.com/rubentalstra/ehrbase-rs/pull/96), v3.0.2                                                                                                                                                |
| W-5                                   | P20 profile-driven optimization (v3.0.3 measured pair: 631.6 vs 316.1 req/s, 2.0×; all 14 classes lower at p50/p99)                                                                                                                                                                                                                                             | released v3.0.3, 2026-07-16; the two open leftovers (group-commit A/B, knee profiler) fold into W-14's close                                                                                                               |
| W-10/W-11/W-12                        | Conformance instrument rewrite · hospital-day benchmark · overload shed layer                                                                                                                                                                                                                                                                                   | 2026-07-13/14                                                                                                                                                                                                              |
| W-1/W-3a/W-3b/W-3c/W-3e/W-3f/W-6..W-9 | citation-discipline sweep · Architecture-Overview study + gap closure · SPS redesign · rest-crate rewrite · platform redesign · folders/AQL-subsumption/paths/EHR_ACCESS rows                                                                                                                                                                                   | merged PRs #72/#74/#76 + `claude/w3f-platform-redesign`, 2026-07-12/13                                                                                                                                                     |

## Standing rules picked up along the way (enforced, not tracked)

- CI must be green on develop at all times (new advisories get dated,
  documented ignores only when no fix is reachable).
- Citable references are ONLY the vendored specs + official external docs —
  **never an internal markdown file** (the ADR layer is deleted; internal
  plan/design files are deleted in the PR that implements them), **and never
  internal tracker IDs** (F-nn/S-nn/G-nn/W-nn/phase markers) in code (owner
  hard rules 2026-07-11 + 2026-07-16 + 2026-07-17); scrub on touch.
- No `use X as Y` import renaming; **zero re-exports** (every import names
  its defining module); `urlencoding` for all percent codecs.
- Every phase ends with a zero-drift ECC run; the baseline only ratchets up.
