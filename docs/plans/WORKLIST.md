# Worklist — the single open-items tracker

Owner-requested (2026-07-12): every open work item lives HERE, one row each,
with status + the governing plan/branch. `current-phase.md` points at this
file; close a row by linking the merged PR. Never track work only in chat.

| # | Item | Status | Where |
|---|------|--------|-------|
| W-1 | **H1 — legacy ADR-citation sweep**: ~1,000 mentions (generated headers via the emitters + regen; ~500 hand-written rewritten to spec citations / explicit spec-silence flags / plain prose) | done | merged [PR #72](https://github.com/rubentalstra/ehrbase-rs/pull/72) 2026-07-12 |
| W-2 | **ECC skip elimination** (owner ruling 2026-07-12: an ECC case may pass, fail, error, or be N/A — NEVER "skipped"). The 26 skips, inventoried: (a) 11 × SM ops with no ITS-REST binding (`list_contributions` ×5, `delete_opt` ×4, `list_queries` ×2) → expose via the existing admin/extension wire and execute, else N/A with citation; (b) 11 × `NativeApiOnly` MSG cases (EHR-extract export/import, TDD) → wire an extract extension API or reclassify N/A pointing at the platform-suite evidence; (c) 4 × `SutConfig` terminology cases → wire the compose SUT to the runner's wiremock TS (host-reachable) + FHIR provider env so they execute; (d) 1 × `sig/pgp-verifies` → a pgp-keyed compose profile. Zero `skipped` outcomes in the final report. | next | this worklist |
| W-3 | **X1 — honest EHRbase vs EHRbase-rs comparison** (owner approved 2026-07-12): upstream ECC run + fairness adjudication register, `tools/benchmark` overhaul (multi-SUT, percentiles, resource footprint), measured comparison page on the docs site. **No false claims — measured numbers only.** | queued (after W-2) | `docs/plans/x1-comparison.md` |
| W-3a | **Architecture Overview study + checklist** (owner directive 2026-07-12, GATED W-4): the full vendored BASE Architecture Overview (masters 00–16) read and distilled into `docs/spec-audit/architecture-overview/CHECKLIST.md` — 149 evidence-backed rows at chapter → section → subsection granularity; gaps filed as W-6/W-7/W-8/W-9 (plus the pre-existing W-4 ADL2 mandate re-confirmed at rows 4.1.1/10.2.1). | done | PR pending merge — `docs/spec-audit/architecture-overview/CHECKLIST.md` |
| W-4 | **ADL2 — full implementation, spec-exact** (owner directive 2026-07-12: "following only the official specs, no deviation"): ADL2 source parser (ADL2 syntax + cADL2 + ODIN sections), AOM2 semantic validation (the full master08 catalogue on parsed artefacts), specialisation flattening, OPT2, template semantics (master10) — replacing the registration-surface-only enforcement from A1 ch14. Oracle: `docs/specs/openehr/AM/docs/{ADL2,AOM2,OPT2}/`. | queued (after W-3a) | plan to be authored as `docs/plans/adl2.md` |
| W-3b | **Architecture Overview gap closure** (owner directive 2026-07-12: fix/implement ALL W-3a gaps properly — greenfield rewrites, baseline schema re-authoring where needed, no quick fixes): executes W-6 + W-7 + W-8 + W-9 as one phase. Plan: `docs/plans/w3b-arch-gaps.md`. ECC run deferred by owner ruling — owed at the next conformance-touching phase. | done | merged [PR #74](https://github.com/rubentalstra/ehrbase-rs/pull/74) 2026-07-12 |
| W-3c | **Subject Proxy Service — complete spec-true redesign + rewrite** (owner directive 2026-07-12: the SPS is the seam for connecting external systems and must follow SM master10 properly and completely; the SM-6 wave-1 skeleton defers the spec's defining behaviours). 13-row gap register (sample history/"tracked over time", currency/freshness, fallback execution, FHIR frame executor, REST extension wire with the spec's JSON/YAML ingestion, `SYSTEM_CALL`-faithful frames, time-series/typed extraction, EHR-Index subject resolution, data-set aliases, `using_app_ids`, manual-variable input channel, spec-orphan classes) + target architecture + 8-step work plan in the design doc. Oracle: `docs/specs/openehr/SM/docs/openehr_platform/master10-subject_proxy_service.adoc` + its UML class files. | done — merged [PR #76](https://github.com/rubentalstra/ehrbase-rs/pull/76) 2026-07-12 (REST wire + ECC SP area → W-3f debt) | design `docs/design/sm-platform/10-subject-proxy.md` |
| W-3e | **`ehrbase-rest` — complete spec-first rewrite against ITS-REST** (owner directive 2026-07-12: the REST crate is the implementation of ITS-REST — rebuild it exactly like the `ehrbase-sm` W-3c rewrite, per spec document: Overview / EHR API / Query API / Definition API / SDT / OAS, folder per spec, files authored from the spec text, everything spec-silent quarantined in `extensions/`; then fix `ehrbase` against both rewritten crates in one pass). Plan: `docs/plans/rest-crate-rewrite.md`. | done — merged [PR #76](https://github.com/rubentalstra/ehrbase-rs/pull/76) 2026-07-12 (nextest/clippy/ECC debt → W-3f) | `docs/plans/rest-crate-rewrite.md` |
| W-3f | **`ehrbase` (the platform crate) — complete redesign** (owner directive 2026-07-12: with `ehrbase-sm` and `ehrbase-rest` spec-structured, the platform crate is the last unstructured one — 34k lines with 1,000-2,000-line files (`vobject.rs` 2,140, `contribution.rs` 1,345, `opt_validation.rs` 1,322, `message.rs` 1,185, `ehr.rs` 1,131 …) and a flat `service/` grab-bag. Same method as W-3c/W-3e: a register first — **the structural oracle is the BASE Architecture Overview** (owner ruling 2026-07-12: `docs/specs/openehr/BASE/docs/architecture_overview/`, already distilled into the 149-row `docs/spec-audit/architecture-overview/CHECKLIST.md`; its chapters define how the concerns compose — e.g. §Integrity places digital signature INSIDE versioning/integrity, so the standalone `signing` module dissolves into the versioning design) cross-read with the SM chapter map + the storage/AQL design docs — then restructure into per-concern subfolders (service/<sm-chapter>/ mirroring `ehrbase-sm`, storage/, aql/ (exists), validation/, versioning/, system_log/ (exists), signing/ (exists)), files authored/split fresh along spec boundaries, zero TODO, dead-code deny already enforced). | done — PR `claude/w3f-platform-redesign` 2026-07-13 (12 spec-first registers → big-bang rewrite → 1440/1440 nextest, clippy clean, ECC 341/315/0 held exactly, CORE+STANDARD PASS) | `docs/plans/w3f-platform-redesign.md` |
| W-3d | **SM platform chapter-register gap closure** (owner directive 2026-07-12: whole SM `openehr_platform` spec audited chapter-by-chapter — register at `docs/design/sm-platform/`, one G-row table per chapter, all cited): execute every G-row of chapters 02–09, 12, 15 (chapter 10 = W-3c). Highest-value rows: I_EHR_STATUS discrete mutators + `get_versioned_directory`/`VERSIONED_FOLDER` (05 G-1/G-2), query multi-EHR scoping + `ehr_id_does_not_exist` (08 G-1/G-2), ADL2 `template_overlay` upload 500 (04 G-7), FHIR terminology `at_date` dropped (12 G-1), EHR-Index error-variant collapse + index/EHR decoupling (07 G-1/G-2/G-8), Message/Admin wire exposure for ECC reachability (09 G-1, 15 G-1 — overlaps W-2), export `OBJECT_REF` namespace rewrite unflagged (09 G-4), archive storage-movement decision (15 G-4), dead-citation scrub (03/06/09/15 hygiene rows). Each row closes in code or as a re-verified cited PORT NOTE. | queued (after W-3c) | `docs/design/sm-platform/README.md` |
| W-6 | **`EHR.folders` multiple hierarchies** (Architecture Overview `master06-design_of_the_ehr.adoc` §The EHR; RM `EHR.folders`): the server enforces a single root FOLDER per EHR (`app/ehrbase/src/service/directory.rs:25`, `contribution.rs:481`) — implement multi-hierarchy folder management (service + admin/extension wire; ITS-REST defines only `/directory`) or record a cited PORT NOTE after a CNF cross-check. Found by the W-3a checklist (row 6.3). | done — merged [PR #74](https://github.com/rubentalstra/ehrbase-rs/pull/74) | `docs/spec-audit/architecture-overview/CHECKLIST.md` |
| W-7 | **AQL archetype-specialisation subsumption** (Architecture Overview `master10-archetypes.adoc` §Design-time Relationships: data of a specialised archetype must be matched by parent-archetype queries): AQL matching is exact case-folded equality (`app/ehrbase/src/aql/sql.rs:632`) — add derivation-aware archetype matching (id-prefix semantics per Archetype Identification). Found by W-3a (row 10.2.2). | done — merged [PR #74](https://github.com/rubentalstra/ehrbase-rs/pull/74) | `docs/spec-audit/architecture-overview/CHECKLIST.md` |
| W-8 | **Paths/locators tail** (Architecture Overview `master11-paths.adoc`): (a) `//` path patterns (§Basic Syntax), (b) positional predicates `[n]` (§Using Positional Parameters), (c) `ehr:` URI grammar parse + local resolution beyond the `Scheme_valid` check (§EHR Reference URIs; `dv_ehr_uri_impl.rs`). Decide per item: implement, or reject with a cited PORT NOTE (AQL 1.1 grammar defines neither (a) nor (b)). Found by W-3a (rows 11.2.1, 11.2.4.3, 11.3.1). | done — merged [PR #74](https://github.com/rubentalstra/ehrbase-rs/pull/74) | `docs/spec-audit/architecture-overview/CHECKLIST.md` |
| W-9 | **EHR_ACCESS / Security-IM realization decision** (Architecture Overview `master07-security.adoc` §§Access Control): EHR_ACCESS is stored/versioned/defaulted but no access decision consults it; no access list, gate-keeper, or Composition privacy levels. openEHR publishes no concrete `ACCESS_CONTROL_SETTINGS` scheme — decide: minimal scheme + per-EHR gate, or a cited PORT NOTE placing authz fully out-of-band (current Cedar RBAC/ABAC in `app/ehrbase-rest/src/access/`). Found by W-3a (rows 5.5.1.5, 7.3.2.2, 7.4.1). | done — merged [PR #74](https://github.com/rubentalstra/ehrbase-rs/pull/74) | `docs/spec-audit/architecture-overview/CHECKLIST.md` |
| W-10 | **`tools/conformance` — full redesign + rewrite of the CNF testing framework** (owner directive 2026-07-13: rethink from the full vendored CNF component up; multi-SUT from day one: ehrbase-rs, upstream EHRbase (Java), BYO endpoint; absorbs the ECC half of X1; Statement/Certificate for every SUT + honest comparison outputs; fairness register, measured numbers only). | **done 2026-07-13** — re-derived baseline **368/333/0/35, CORE+STANDARD PASS** (`docs/conformance/ehrbase-rs/`); upstream 2.34.0 recorded as DATA + `COMPARISON.md`; 7 real server defects fixed in spec-cited commits; per-case triage of the 162 upstream failures moves to X1 | `docs/plans/w10-conformance-redesign.md` |
| W-11 | **`tools/benchmark` — complete rewrite: realistic hospital-day stress instrument** (owner directive 2026-07-13, PRIO 1 after W-10: real templates + generated clinical data; mixed read/write/update/versioning + AQL workload shaped like a hospital day; multi-SUT like the ECC runner; measures latency percentiles, throughput, CPU + RAM footprint "and everything in between that makes sense"). Absorbs X1's benchmark half. | **done 2026-07-14** — hospital-day instrument shipped (CKM template pack, 16-class CO-corrected latency, CPU/RSS/storage/cold-start, knee ladder with generator-bound + died-under-load detection, SVG-charted reports + cross-SUT comparison); hour-profile baselines + knees committed for both SUTs; found + fixed: 2 example-generator defects (spec-cited), W-12 OOM (shed layer), ECC-TPL-017 added; PR pending merge | `docs/plans/w11-benchmark-rewrite.md` |
| W-12 | **Overload behaviour / backpressure** (found by the W-11 knee ladder, 2026-07-13): under sustained offered load beyond DB capacity the server queues requests unboundedly (each holding its state while waiting on `sqlx` pool acquire, 30 s timeout) until the container is OOM-killed (exit 137 at ~640 req/s scheduled with a 10-connection pool). Design a proper ingress backpressure layer: bounded concurrency + load shedding (tower `load_shed`/`concurrency_limit` or equivalent), a sane pool-acquire timeout under overload, and a documented 429/503 overload contract (ITS-REST declares no overload semantics — flag as our own design). Verify by rerunning the knee ladder: the server must degrade with clean errors, never OOM. | **done** — shipped in W-11/P20: bounded in-flight concurrency + load shed (`ehrbase-rest::overload`, 503 + Retry-After, `EHRBASE_REST_MAX_IN_FLIGHT`, default 256); verified by the P20 knee ladders — the server degrades with clean 503s under storm load, no OOM (benchmark runs raise the cap for admission parity with upstream) | `app/ehrbase-rest/src/overload.rs` |
| W-5 | **P20 — re-plan, then execute** (owner ruling 2026-07-12: measure first). Re-planned 2026-07-14 from the W-11 measured evidence (write-path saturation, AQL scale degradation); owner go — **in progress** (T1 profiling harness first) | in progress | `docs/plans/phase-20-optimization.md`, branch `claude/p20-optimization` |

## Standing rules picked up along the way (enforced, not tracked)

- CI must be green on develop at all times (audit/deny/machete fixed in PR #71;
  new advisories get dated, documented ignores only when no fix is reachable).
- Spec-only citations in code (never ADRs); scrub on touch.
- No `use X as Y` import renaming; `urlencoding` for all percent codecs.
- Every phase ends with a zero-drift ECC run; the baseline only ratchets up.
