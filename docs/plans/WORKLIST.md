# Worklist — the single open-items tracker

Owner-requested (2026-07-12): every open work item lives HERE, one row each,
with status + the governing plan/branch. **This file is the single entry
point** (the former `current-phase.md` pointer was retired 2026-07-17);
close a row by linking the merged PR, and delete the row's plan file in the
PR that implements it. Never track work only in chat.
Fully refreshed 2026-07-16 (post platform-rewrite; stale rows pruned to the
closed table at the bottom).

## Open

| #       | Item                                                                                                                                                                                                                                                                                                                                                                                         | Status              | Where                                                             |
|---------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------|-------------------------------------------------------------------|
| TRACKER | **Retire this file: the worklist moves to GitHub Issues** (owner 2026-07-20 — **PRIO ONE for the next clean Fable 5 session**). Why: PR linking with auto-close (`Closes #N`), native state + timeline (the history problem the Closed table approximates by hand), labels/milestones we already half-use (the `spec-update` taxonomy, issue #94, SPEC-WATCH opens issues), and per-item discussion threads instead of ever-growing status cells (see the ADL2 row below for the failure mode). The look-and-feel to implement — INDUSTRY-STANDARD taxonomy, nothing invented (owner correction 2026-07-20: no house `worklist` label — the OPEN ISSUE LIST **IS** the tracker; that is what issue trackers are): (1) GitHub's native **Issue Types** (Bug / Feature / Task) where the repo/org supports them, else the prebuilt default labels (`bug`, `enhancement`, `documentation`, `question`, `duplicate`, `invalid`, `wontfix`, `help wanted`, `good first issue`) — mapped to our conventional-commit types (fix↔bug, feat↔enhancement, docs↔documentation) with minimal supplements ONLY where no default exists (`chore`, `refactor`, `perf`, `ci`); (2) our existing DOMAIN labels stay (`spec:RM…`, `spec-update`, `spec-impact:*` — purposeful area labels, the standard pattern); (3) **milestones = releases** (v3.3.0…); (4) issue body = the contract + exit-criteria checklist, phases as task-list checkboxes; PRs declare `Closes #N`; merge auto-closes; priority via milestone ordering + a `P1`-style label only if genuinely needed — prefer the board order. Agent-workflow rewiring (the real work): (a) session-start orientation switches from reading this file to `gh issue list --state open` (the whole open list — no marker label needed) — the SessionStart hook injects it (no offline fallback — owner 2026-07-20: the agents work online by definition; a failed `gh` call just surfaces its error); (b) the `phase_gate` Stop hook's "worklist touched" check becomes "a commit referencing an issue exists or an issue comment/checkbox was posted"; (c) the `next-task`/`phase-done`/`phase-status` skills + root CLAUDE.md workflow section + the memory entries rewrite against issues; (d) **plan files stay unchanged** — deep working plans (like `docs/plans/adl2.md`) keep their delete-on-implementation lifecycle; the issue replaces the tracker ROW, not the plan document; (e) migration: every open row here becomes an issue (EXAMPLE folds into the existing #94 lineage), the Closed table is archived as a historical file, `docs/PROGRESS.md` STAYS as the narrative per-phase record (issues track state; PROGRESS records what was built); (f) single-writer discipline noted: agents edit issue state only via `gh`, the same review rules apply. Exit: this file's Open table is empty except a pointer at the issue board, the hooks/skills/docs all speak issues, and one full row lifecycle (open → phases → PR → auto-close) has been proven end-to-end. | queued — **PRIO ONE next session** | this worklist (its own retirement) |
| SCRUB   | **Tracker-ID comment scrub** (owner hard rule 2026-07-16: no F-nn/S-nn/G-nn/W-nn/wave/phase markers anywhere in code — only `docs/specs/openehr/` citations): ~700 occurrences across ~170 files; partial slice landed; the six scrub agents relaunch when the session limit resets (file lists staged in the session scratchpad).                                                           | parked (rate limit) | this worklist                                                     |
| UI-2    | **Admin console — design overhaul + 100% feature completeness** (owner 2026-07-17: "we need an full 100% compleet admin UI"): a real visual design system (layout, typography, spacing, color, dark mode, empty/loading/error states) replacing the bare default-widget look, plus the missing features — query result export, EHR creation, composition commit/update from the UI, template + stored-query admin actions, builder order-by/column picker, table pagination, tab-state-in-URL, action feedback (toasts). Screen inventory in flight; screenshots re-captured per the ui-screenshot-guard.                                                                                                                                                             | in progress         | branch `claude/admin-ui-design-overhaul` (after the seeded-journeys branch merges) |
| EMIT-ENUM | **Main `emit` target drops enumeration items** (found 2026-07-19 during the emit-rm-model work, reported per the scope guard): BMM enumeration classes lose their literal names/values in the generated spec crates — e.g. `am24` `ConstraintStatus` emits as a transparent `i32` newtype instead of a typed literal set (required/extensible/preferred/example). Violates the completeness hard rule (the BMM defines the items; the generator must emit them). Fix in the emitter + regenerate; NOTE: changes generated type shapes workspace-wide (consumers pattern-match today on magic numbers) — bounded but cross-cutting; may land standalone or fold into the CODEGEN rewrite row (owner's call at pickup). Wire risk: enum serde must stay byte-identical on the wire (fidelity gates + ECC zero-drift prove it). Full implementation blueprint researched 2026-07-19 (13-class inventory, wire-tolerance evidence — `Other` catch-all REQUIRED: out-of-range VERSION_STATUS tokens are load-bearing in HRID parsing —, exact emitter change points, consumer inventory): `docs/plans/emit-enum.md`. Implement on `feat/adl2` after the in-flight phase lands. | queued (blueprint ready) | plan `docs/plans/emit-enum.md` |
| SPEC-WATCH | **Scheduled openEHR spec-update watcher** (owner 2026-07-19): a cron workflow (GitHub Actions schedule, 1–2×/week) that watches for newly COMPLETED openEHR specification changes and turns each into a triageable issue, so spec updates never pass us by: (1) poll the public openEHR Jira (openehr.atlassian.net — the SPEC* projects: SPECRM/SPECBASE/SPECLANG/SPECAM/SPECQUERY/SPECITS/SPECTERM/SPECPR…) for issues newly moved to Completed/Resolved since the last run; (2) cross-check the upstream specifications-* repos' Amendment Record tables (the `Issue / Details / Raiser / Completed` tables each spec document carries — e.g. "1.5.3 SPECLANG-10: Fix typos in ODIN") for entries newer than our vendored pins in `docs/VERSIONS.md`; (3) open one GitHub issue per completed spec change (labelled `spec-update`, carrying the Jira key + affected component + our current pin) for conformance-impact triage → re-vendor → regenerate → implement where behaviour changes. State (last-seen watermark) kept in-repo or via the issues themselves (dedup by Jira key). Keeps us spec-compliant CONTINUOUSLY, not just at pin bumps. **The GitHub label taxonomy EXISTS (created 2026-07-19):** `spec-update` (red, the trigger) · `spec:RM/BASE/AM/LANG/QUERY/TERM/ITS/SM/CNF` (green, component) · `spec-impact:behaviour` / `spec-impact:docs-only` / `spec-impact:none` (triage outcomes) — the workflow labels every issue it opens with `spec-update` + its component; triage adds exactly one `spec-impact:*`. | queued (labels live) | this worklist |
| ADL14 | **ADL 1.4 surfaces — spec audit + rewrite-what-fails onto the engine** (owner 2026-07-20, prompted by the ADL2 build: "it feels like we did not do 1.4 fully to the official specs"): audit-first against the vendored `docs/specs/openehr/AM/docs/{ADL1.4,AOM1.4}/` (1.4 has its OWN normative documents + validity rules — not "old ADL2"). The three surfaces, pedigree-split: (1) `openehr-its::opt14` XML OPT ingestion — generated from the vendored XSDs, corpus round-tripped, ECC-proven → expect audit-pass; (2) `openehr-am::am14` — BMM-generated → expect pass; (3) **the suspect: the `I_DEFINITION_ADL14` text seam** (`app/ehrbase/src/service/definition/adl14.rs`) — pre-engine, likely the same registration-subset-probe pattern whose ADL2 twin died in the ADL2 row's REST phase. Rewrite target: `openehr-adl` ALREADY parses ADL 1.4 text (`Dialect::Adl14` — shared lexer/cADL with the 1.4 tolerances + domain blocks, corpus-proven on the 21 vendored `.adl` files) — the seam consumes it directly; gap to scope in-audit: an AOM 1.4 validity catalogue (1.4's own, smaller rule set) vs validating post-conversion via the ADL2 catalogue (the converter exists — decide with citations). Also in-audit: whether upload should offer/store the 1.4→2 conversion (the in-CDR migration story the ADL2 row built the core for). **FOLDED INTO THE ADL2 PR (owner 2026-07-20: "it's part of the full ADL setup")** — runs after the REST-integration phase lands (same files; no concurrent same-module agents), before the row's close, so ONE ECC run covers the complete ADL 2.4+1.4 story. | in this PR (after the REST phase) | branch `feat/adl2-flattener` |
| SITE-CONF | **Website conformance status goes dynamic — stale numbers are forbidden** (owner 2026-07-20: the ECC catalogue keeps growing/ratcheting while the landing page stalls; "stalled information is very very bad"): the site must derive every conformance claim from the runner-generated artifacts, never hand-typed numbers. (1) single source of truth = `docs/conformance/ehrbase-rs/results.json` + the ECC badge `.json` renders — the site BUILD ingests them (the landing-page executed/passed/areas/OPTIONS-capability numbers rendered from the artifact at build time; an mdBook preprocessor or a generation step in the Pages workflow); (2) badges via the shields.io endpoint scheme pointing at the raw badge JSON URLs on the default branch — auto-updating on every merged ECC ratchet, zero manual edits; (3) a CI guard: the site build FAILS if a hard-coded conformance number appears in the landing/book sources (grep-gate) — the stale-information failure mode becomes impossible, not just discouraged; (4) sweep the existing landing/book pages for currently-stalled numbers and convert them. | queued | this worklist |
| BASE-ISO8601-ORD | **ISO 8601 temporal ordering in `openehr-base`** (registered at the ADL2 A11 close): the generated `openehr-base` `Iso8601_Date`/`Time`/`Date_Time`/`Duration` types provide no ordering (`PartialOrd`/compare), so `Interval::has`/`contains` cannot be evaluated for temporal value spaces. Implement ISO 8601 temporal ordering as hand-written spec behaviour in sibling `*_impl.rs` files (BASE `foundation_types/time` — partial-date semantics, timezone normalisation, duration comparison; spec: `docs/specs/openehr/BASE/docs/foundation_types`). This unblocks the temporal half of VOBAV assumed-value interval containment in `openehr-adl` (`validate::phase1::check_primitive_assumed`) and any temporal C_ORDERED conformance. Safety-sensitive (clinical date comparison) — needs its own focused, tested implementation. | queued | this worklist |
| ADL2-VETDF | **External terminology-service resolver seam for ADL2 archetype term-binding validation** (registered at the ADL2 A11 close): VETDF (`master03` §Validity Rules) requires that a code bound to an *external* terminology (SNOMED CT, LOINC, …) exists in that terminology. `openehr-adl` is a network-free spec engine (no app/SQL/REST) and cannot hold a live terminology-service client, so VETDF is present as a catalogue vocabulary variant but not raised there. Thread a terminology-service resolver seam (like the `RmModel` seam) through the validator and wire the application's terminology service (bundled + external FHIR TS, `ehrbase::service::terminology`) so external archetype term bindings are validated. Bundled/openEHR-internal bindings are already covered by VTTBK/VTCBK key validity. | queued | this worklist |
| ADL2-OPT14 | **OPT-1.4 → ADL2 conversion front end** (registered at the ADL2 A11 close): the in-CDR 1.4→2 converter (`openehr_adl::adl14`) today takes an assembled `am24` *source archetype*, so only stored 1.4 source archetypes convert (`EhrbaseService::adl14_convert_to_adl2`). Build an OPT-1.4 (`template_store`, XML/`opt14` DTOs) → `am24` reader front end so stored 1.4 operational templates also convert. No wire currently exposes 1.4→2 conversion (service-only capability), so this is out of the ADL2 row's scope; the `adl14` converter itself (source-archetype path) is complete. | queued | this worklist |
| UI-3    | **Admin console — full as-shipped review + spec-compliance audit + the next plan** (owner 2026-07-18): after develop is green and v3.2.0 is released, a fresh Fable 5 session walks the COMPLETE shipped admin-ui and its plan (`docs/plans/admin-ui-overhaul.md` — kept, NOT deleted, per owner), audits every surface the console touches against the official specs (openEHR ITS-REST/RM + IHE/FHIR where relevant), and authors a new plan file for the next iteration of the best possible spec-following admin UI. | queued (after v3.2.0) | plan to be authored by the follow-up session                       |

## Closed (recent; full history in `docs/PROGRESS.md` + git)

| #                                     | Item                                                                                                                                                                                                                                                                                                                                                            | Closed                                                                                                                                                                                                                     |
|---------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| ADL2 | ADL 2.4 + ADL 1.4 end to end: the openehr-adl engine (parser/AOM/validity catalogue/flattener/OPT2/1.4→2 converter), the wire integration (shadow validator deleted; 1.4 validated as 1.4), both-dialect example generation (issue #94 closed; two conformance defects found+fixed en route), 340/340 corpus coverage, zero unaccounted TODOs, the ECC Adl2Provisioning+AqlTerminology areas; close run 402/367/0 Core+Standard PASS, Options OBTAINED — full record in `docs/PROGRESS.md` ADL2 row | [PR #131](https://github.com/rubentalstra/ehrbase-rs/pull/131), 2026-07-20 |
| EXAMPLE | Issue #94 — the example generator rewritten for both dialects: fully-valid deterministic compositions (77/78 corpus at both committable levels; the 78th proven contradictory and pinned), the am24→WebTemplate front end, the raised full-validation corpus bar | [PR #131](https://github.com/rubentalstra/ehrbase-rs/pull/131), 2026-07-20 |
| CODEGEN | The generation subsystem rewritten (R0–R6): four-stage pipeline in `tools/openehr-codegen`, spec-cited declarative decision maps + tested emitter invariants, the serde-free native canonical-JSON codec (`openehr-derive` deleted, −12k lines), BMM constants + the 18 generated RM invariant cores (archie spellings dead), EMIT-ENUM + emit-rm-model completeness folded in; wire proven unchanged at every phase; full record in `docs/PROGRESS.md` CODEGEN row; ECC 386/351/0 zero-drift | [PR #130](https://github.com/rubentalstra/ehrbase-rs/pull/130), 2026-07-20 |
| INV-UNIFY | The 30 terminology-backed RM invariants enforced at the openehr-its dispatcher via the openehr-term bundle (corpus-clean over 3000+ nodes); aggregate exclusions citation-pinned; the fast-path-unification half evaporated by design (single-source generated cores) | [PR #130](https://github.com/rubentalstra/ehrbase-rs/pull/130), 2026-07-20 |
| OAPI                                  | Served-OpenAPI completeness sweep — all 162 utoipa declarations spec-exact against BOTH ITS-REST halves (op YAMLs + the overview prose), reconciliations recorded per module, plus the owner-directed wire fixes (disabled admin → the OAS-declared 405, tags updates honour Prefer with the spec-default 204, demographic Last-Modified, relationship return=identifier, ehr_create_with_id 200→201 correction), the structural completeness gate test, and a tracker-ID/internal-citation scrub of touched files; workspace 2002/2002, ECC 386/351/0 zero-drift                                                                                                             | [PR #129](https://github.com/rubentalstra/ehrbase-rs/pull/129), 2026-07-19                                                                                                                                 |
| DIR                                   | DIRECTORY full spec audit + total rewrite — the five ITS-REST operations audited first-hand (op YAMLs + overview prose + RM master05 + CNF master09) with the real gaps closed (`?path=` on the version read, ETag+Last-Modified on directory reads, create-after-delete opens a new hierarchy via the live-slot probe, complete utoipa declarations), 21-test spec-cited wire battery, and the console Directory tab rebuilt into a complete experience (structured tree editor, version history + restore, at-time, path query, delete+recreate) with 3 new e2e journeys; en route: wasm disposal panics + hydration-wiped finder inputs root-caused and fixed (owner: pre-existing defects fixed properly too); workspace 1998/1998, battery 16/16, ECC 386/351/0 zero-drift                                                                                                             | [PR #128](https://github.com/rubentalstra/ehrbase-rs/pull/128), 2026-07-19                                                                                                                                 |
| TESTPERF                              | Testing-framework full redesign + rewrite — the shared `tools/testkit` harness (one PG18 server + fingerprinted template database + clone per test) replaces per-test containers across all 50 DB test files; measured 791 s → 329 s full-workspace wall (2.4×), the 300 s route-coverage offender → 5.0 s, 1958/1958 green, coverage unchanged; CI runs zero docker-in-docker test containers; ECC 386/351/0 zero-drift                                                                                                                                                                                             | [PR #127](https://github.com/rubentalstra/ehrbase-rs/pull/127), 2026-07-18                                                                                                                                 |
| ATNA                                  | ATNA audit-trail full redesign (dual DICOM+BALP format with the ITI-20 MSGID/EventID conformance fixes; local Audit Record Repository ON by default in the new `audit` schema; ITI-20 ATX:FHIR Feed with outbox durability; ITI-81 `GET /fhir/r4/AuditEvent` admin retrieval; ITI-19 native mTLS via `[server.tls]`; `[atna]`→`[audit]` config redesign; dedicated book chapter; plan file deleted per the lifecycle — the admin-console audit-log browser landed on the design kit with e2e journeys + book screenshots; ECC deferred by owner to the joint ATNA+UI-2 convergence run)                                                                | branch `claude/atna-audit-rewrite`, 2026-07-18                                                                                                                                                                             |
| W-21                                  | Release v3.1.0 + v3.1.1 (minor bump: Simplified Formats rewrite + governance overhauls); en route: the tenant-GUC pool-stamp defect fixed with a deterministic regression test (#110), the release.yml binary-asset build fixed after the five-workflow audit (v3.1.0 published notes-only; v3.1.1 carries the tarball assets, prerelease until owner sign-off) | [PR #109](https://github.com/rubentalstra/ehrbase-rs/pull/109) / [#110](https://github.com/rubentalstra/ehrbase-rs/pull/110) / [#111](https://github.com/rubentalstra/ehrbase-rs/pull/111), tags v3.1.0+v3.1.1, 2026-07-17 |
| ADMIN-UI                              | The Leptos admin console shipped end-to-end (see `docs/PROGRESS.md` ADMIN-UI row: full screen set, the AST-emitting Query Builder, the Rust-native browser E2E battery green 7/7 with 7 real defects found+fixed pre-merge, third OCI image + compose + book chapter with harness-captured screenshots, ECC zero-drift)                                                                    | branch `claude/admin-ui`, 2026-07-18                                                                                                                                                                                       |
| FLAT                                  | Simplified Formats spec-exact greenfield rewrite (openehr-flat + REST negotiation from the STABLE ITS-REST spec; ECC-SF area added; plan file deleted per the lifecycle)                                                                                                                                                                                        | [PR #108](https://github.com/rubentalstra/ehrbase-rs/pull/108), 2026-07-17                                                                                                                                                 |
| W-16                                  | Issue #95 — format support via Accept header (subsumed by the FLAT rewrite: strict RFC 9110 negotiation on every relevant endpoint, 406/415 correctness, book docs updated)                                                                                                                                                                                     | [PR #108](https://github.com/rubentalstra/ehrbase-rs/pull/108), 2026-07-17                                                                                                                                                 |
| W-13                                  | Configuration redesign — one TOML file                                                                                                                                                                                                                                                                                                                          | merged [PR #96](https://github.com/rubentalstra/ehrbase-rs/pull/96), v3.0.2                                                                                                                                                |
| W-5                                   | P20 profile-driven optimization (v3.0.3 measured pair: 631.6 vs 316.1 req/s, 2.0×; all 14 classes lower at p50/p99)                                                                                                                                                                                                                                             | released v3.0.3, 2026-07-16; the two open leftovers (group-commit A/B, knee profiler) fold into W-14's close                                                                                                               |
| W-10/W-11/W-12                        | Conformance instrument rewrite · hospital-day benchmark · overload shed layer                                                                                                                                                                                                                                                                                   | 2026-07-13/14                                                                                                                                                                                                              |
| W-1/W-3a/W-3b/W-3c/W-3e/W-3f/W-6..W-9 | citation-discipline sweep · Architecture-Overview study + gap closure · SPS redesign · rest-crate rewrite · platform redesign · folders/AQL-subsumption/paths/EHR_ACCESS rows                                                                                                                                                                                   | merged PRs #72/#74/#76 + `claude/w3f-platform-redesign`, 2026-07-12/13                                                                                                                                                     |

## Standing rules picked up along the way (enforced, not tracked)

- CI must be green on develop at all times (new advisories get dated,
  documented ignores only when no fix is reachable).
- Citable references are ONLY the vendored specs + official external docs —
  **never an internal markdown file** (the ADR layer is deleted; internal
  plan/design files are deleted in the PR that implements them), **and never
  internal tracker IDs** (F-nn/S-nn/G-nn/W-nn/phase markers) in code (owner
  hard rules 2026-07-11 + 2026-07-16 + 2026-07-17); scrub on touch.
- No `use X as Y` import renaming; **zero re-exports** (every import names
  its defining module); `urlencoding` for all percent codecs.
- Every phase ends with a zero-drift ECC run; the baseline only ratchets up.
