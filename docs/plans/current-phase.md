- **Next phase: P11 — REST server foundation + auth** (`docs/plans/phase-11-rest-server-auth.md`). The `ehrbase-rest` axum app implementing the generated ITS-REST server traits, tower-http stack, content negotiation, Basic + OAuth2/OIDC.
- **Read first:** `docs/ADRs/ADR-008-greenfield-pg18-storage.md` (the pivot — own internals, openEHR spec conformance as the target), then ADR-004/005 (generated foundation). ADR-006/007 partially superseded.

- **Where we are.** Foundation (00–08) done. **P09 done** (db infra: settings/pool/two-schema migrators/testcontainers). **P10 done (2026-07-05):** the greenfield schema is live — `migrations/ehr/0001_schema.sql` (unified per-version `node` table with nested-set indexing + canonical JSON fragments, temporal `vo_version` with `WITHOUT OVERLAPS`, ehr/audit/contribution/template_store/stored_query/item_tag) + `migrations/ext/0001_openehr_functions.sql` (spec-formula `openehr_magnitude` + ISO-8601 helpers, IMMUTABLE for expression indexes); `ehrbase::storage` is the lossless node codec (corpus-proven, incl. DB round-trip + CONTAINS check); `db::iden` matches; the spike harness + measured results live in `tests/storage_spike.rs` + the P10 phase file.

- **Session goal (P11).** Implement the 5 generated `*Api` traits (`openehr-its/src/rest/generated/`: ehr ~35 ops, demographic ~40, definition 13, query 6, admin 2) as an axum app in `ehrbase-rest`: router built from the `ROUTES` const tables; stubs return typed `ApiError::NotImplemented` until P12 fills them; full `tower-http` stack (trace/cors/compression/timeout/request-id/sensitive-headers/catch-panic/normalize-path); content negotiation (canonical JSON + XML via `openehr-its`); Basic (`argon2`) + OAuth2/OIDC bearer (`jsonwebtoken`/`oauth2`/`openidconnect`) middleware; `figment` config; `utoipa` Swagger UI + OAS drift check; `/rest/status` + health. **Docs-first:** verify `oauth2` 5.x / `openidconnect` 4.x / `tower-sessions` / `axum-login` pins before first use.

- **App build order:** P09 ✅ → P10 ✅ → **P11** REST+auth → P12 service layer (+ smoke conformance wiring) → P13 templates → P14 WebTemplate → P15 validation → P16 AQL engine (our IR + BMM-generated RM model) → P17 FLAT/EhrScape → P18 integration → P19 openEHR conformance → P20 optimization → P99 cleanup/release.

- **Discipline:** compiling + clippy-clean + tested per phase; `thiserror` libs / `anyhow` binary; no `unwrap`/`expect` outside tests; consume `openehr-*` types; never hand-edit `// @generated`; official CLIs for tool-managed artifacts; tick checkboxes + commit on `claude/phase-NN-*`. EHRbase Java is out of the tree (ADR-008); the openEHR specs are the authority.
