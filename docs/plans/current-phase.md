- **Next phase: P12 — Service layer (orchestration, versioning, contributions, audit)** (`docs/plans/phase-12-service-layer.md`). Fill the P11 REST handlers with the real service: EHR / EHR_STATUS / COMPOSITION / CONTRIBUTION (and DIRECTORY/FOLDER) create/get/update/delete on the P10 node-codec storage, with versioning + a CONTRIBUTION + AUDIT_DETAILS on every write, in `sqlx` transactions.
- **Read first:** `docs/ADRs/ADR-008-greenfield-pg18-storage.md` (own internals — the node table + temporal `vo_version`, no `_history` pairs; `ALL_VERSIONS` supported), then ADR-004/005/006. Note: the P12 phase file's "v2 tables / current + `_history` / rm-db-format" wording predates ADR-008 — reconcile it to the greenfield schema (`node` + `vo_version`, `ehrbase::storage` codec) at the start of P12.

- **Where we are.** Foundation (00–08) done. **P09 done** (db infra). **P10 done (2026-07-05):** greenfield schema (`node` + temporal `vo_version` + `ext` fns) + the lossless node codec (`ehrbase::storage`), corpus- + PG18-proven. **P11 done (2026-07-05):** `ehrbase-rest` is a full axum app implementing all five generated ITS-REST `*Api` traits (~96 ops mounted from the `ROUTES` tables via a generic dispatcher + a type-directed `*Params` deserializer); full `tower-http` stack; JSON/XML content negotiation (`openehr-its`); **Basic (argon2) + OAuth2/OIDC bearer auth** (jsonwebtoken over a JWKS; resource-server role) as one middleware (401/`WWW-Authenticate`; coarse admin-scope → 403); `figment` config; Swagger UI + `/rest/status`/`/health`/`/management/info`; the `ehrbase` binary boots & serves. Handlers return `NotImplemented`. 48 crate tests + 107/107 workspace green, clippy-clean. Design docs added: `docs/enterprise/atna-audit.md`, `docs/observability.md`, `docs/terminology-validation.md`.

- **Session goal (P12) — full implementation, e2e-tested on real PG 18.4 (owner directive: no stubs, genuinely e2e-testable):** implement the service layer behind the P11 handlers for **EHR, EHR_STATUS, COMPOSITION, CONTRIBUTION** (and DIRECTORY/FOLDER): create/get/update/delete decomposed into the `node` table via the P10 codec; versioning via the temporal `vo_version` table (`OBJECT_VERSION_ID`, `LATEST_VERSION`/`ALL_VERSIONS`, revision history); a `CONTRIBUTION` + `AUDIT_DETAILS` row per write; optimistic concurrency (`If-Match`); stored-query + item-tag CRUD. Wire into the trait impls on `AppState` (swap the `NotImplemented` stubs for service calls); the dispatcher already hands handlers canonical-JSON `Value` bodies (incl. XML-decoded RM payloads), and typed XML responses light up via `negotiate::respond_negotiated`. **Integration tests against a testcontainers PG 18.4** driving create→retrieve→update→version over HTTP. Composition *validation* (P15) and AQL (P16) stay out; call into validation from the write path once it lands.

- **App build order:** P09 ✅ → P10 ✅ → P11 ✅ → **P12** service layer → P13 templates → P14 WebTemplate → P15 validation → P16 AQL engine → P17 FLAT/EhrScape → P18 integration → P19 openEHR conformance → P20 optimization → P99 cleanup/release.

- **Discipline:** compiling + clippy-clean + tested per phase (`cargo nextest`, testcontainers PG18.4 where DB is involved; containers must not outlive tests); `thiserror` libs / `anyhow` binary; no `unwrap`/`expect` outside tests; consume `openehr-*` types; never hand-edit `// @generated`; official CLIs for tool-managed artifacts; tick checkboxes + commit on `claude/phase-NN-*`. EHRbase Java is out of the tree (ADR-008); the openEHR specs are the authority.
