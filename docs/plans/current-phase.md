- **Current phase: P12 — Service layer (in progress)** (`docs/plans/phase-12-service-layer.md`). The `ehrbase::service` turns ITS-REST calls into persisted, versioned openEHR data on the greenfield `node`/`vo_version` store.
- **Read first:** `docs/ADRs/ADR-008-greenfield-pg18-storage.md` (own internals — the node table + temporal `vo_version`, no `_history` pairs; `ALL_VERSIONS` supported), then ADR-004/005/006.

- **Where we are.** Foundation (00–08) done. **P09/P10 done (2026-07-05):** greenfield schema (`node` + temporal `vo_version` + `ext` fns) + the lossless node codec (`ehrbase::storage`), corpus- + PG18-proven. **P11 done (2026-07-05):** `ehrbase-rest` axum app — all five generated `*Api` traits mounted (~96 ops via a generic `ROUTES` dispatcher + type-directed `*Params` deserializer); full `tower-http` stack; JSON/XML negotiation; Basic + OAuth2/OIDC bearer auth (401/403); `figment` config; Swagger UI + status/health/info; binary boots. **P12 in progress (2026-07-05):** dependency-inversion `Backend` seam; `ehrbase::service` implements EHR / EHR_STATUS / COMPOSITION / DIRECTORY(FOLDER) / CONTRIBUTION on the shared `vobject` machinery (decompose→nodes + temporal `vo_version` + contribution + audit in one tx; read = reassemble); temporal versioning + time-travel (`version_at_time`); `contribution_create` (atomic multi-version); revision history; committer taken from the authenticated principal; the binary boots the DB-backed service. e2e on testcontainers PG 18 (5 service tests) + `ehrbase-rest` 48 tests; clippy/fmt clean.

- **P12 done so far:** the versioned-object CRUD (EHR/EHR_STATUS/COMPOSITION/DIRECTORY/CONTRIBUTION) + temporal versioning + time-travel; `contribution_create` (atomic multi-version); revision history; **`ehr_get_by_subject`** (subject-indexed lookup); **stored-query CRUD** (`stored_query`); **item-tag CRUD** (`item_tag`, COMPOSITION + EHR_STATUS targets); committer-from-principal. 8 service e2e tests on PG 18 + `ehrbase-rest` 48.
- **P12 done (all items):** typed XML *responses* for single spec-typed RM objects (COMPOSITION / EHR_STATUS / EHR / FOLDER) landed via `negotiate::respond_rm` (re-types the canonical-JSON value into its `openehr-rm` type at the response edge; VERSION-family/lists/tags stay JSON-only). **Out (later phases):** composition validation (P15), templates/OPT-XML ingestion (P13), demographic (RM phase), AQL/query execution (P16), fine-grained RBAC (Stage 2).

- **App build order:** P09 ✅ → P10 ✅ → P11 ✅ → **P12** service layer → P13 templates → P14 WebTemplate → P15 validation → P16 AQL engine → P17 FLAT/EhrScape → P18 integration → P19 openEHR conformance → P20 optimization → P99 cleanup/release.

- **Discipline:** compiling + clippy-clean + tested per phase (`cargo nextest`, testcontainers PG18.4 where DB is involved; containers must not outlive tests); `thiserror` libs / `anyhow` binary; no `unwrap`/`expect` outside tests; consume `openehr-*` types; never hand-edit `// @generated`; official CLIs for tool-managed artifacts; tick checkboxes + commit on `claude/phase-NN-*`. EHRbase Java is out of the tree (ADR-008); the openEHR specs are the authority.
