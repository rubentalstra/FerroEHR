- **Next phase: P10 — rm-db-format** (`docs/plans/phase-10-rm-db-format.md`). The RM↔JSONB row-per-locatable bridge, step 2 of the Stage-1 EHRbase **application** build.
- **Read first:** `docs/ADRs/ADR-006-application-port-philosophy.md` (the pivot), ADR-007 (squashed baseline migrations + equality gate), then ADR-004 (spec crates generated) + ADR-005 (ITS generated). The phase files + `docs/PROGRESS.md` are authoritative over `PORT_MASTER_PLAN` prose where they differ (the master plan §10 carries an amendment note pointing here).

- **Phase numbering (2026-07-04):** the phase files were renumbered into one clean `00→20, 99` sequence so file order = actual work order. Foundation (done) = `00–08`; Stage-1 application build (in dependency order) = `09–20`; cutover = `99`.

- **Where we are.** The openEHR **foundation (00–08) is done and compiling** (generated spec crates, canonical JSON+XML, ITS-REST contract, AQL parser; `codegen-drift` CI guards it). **P09 persistence is done (2026-07-05):** `ehrbase::db` provides figment-based `DbSettings`, a search-path-initialized `PgPool`, `run_migrations` (bootstraps `ext`/`ehr` schemas + extensions, runs the two squashed `0001_baseline.sql` migrators, ext first), and `sea-query` `Iden` enums for all 17 final-state v2 tables. The legacy EHRbase Flyway chain is preserved at `crates/ehrbase/tests/resources/legacy_schema/` and a schema-equality test proves the baseline reproduces its exact end state (ADR-007). Binder is **`sea-query-sqlx`** (not `sea-query-binder`); jiff↔Postgres via `jiff-sqlx`; new migrations only via `sqlx migrate add --sequential`.

- **The goal (ADR-006).** Build the **best-possible EHRbase in Rust**: openEHR-spec-conformant, behavior-compatible with EHRbase at the REST/AQL surface, as a **modern idiomatic application** that consumes the generated `openehr-*` crates directly as its domain model. Use proper crates — `axum`+`tower-http`, `sqlx`+`sea-query`/`sea-query-sqlx` (NOT sea-orm), `oauth2`/`openidconnect`/`jsonwebtoken`/`argon2` for **Basic + OAuth2/OIDC auth** (Stage 1; RBAC is Stage 2), `utoipa`, `moka`, `jiff`, `testcontainers` PG18. Do **not** hand-roll what a good crate provides, and do **not** mirror EHRbase's Java class structure. The bespoke server logic no crate provides — the AQL engine (AST→ASL→SQL), versioning, validation, RM↔JSONB mapping — is written idiomatically **following EHRbase's proven algorithm as the reference** (`crates/ehrbase/src/{aql,service,rm_db_format}/` Java is the read-only reference). The **parity harness** (minimal harness from P12; full gate at P19) is the acceptance instrument.

- **Session goal (P10).** Port EHRbase's rm-db-format algorithm idiomatically (`crates/ehrbase/src/rm_db_format/` Java is the reference): decompose an RM graph (canonical JSON via `openehr-its::json` — the single source of truth, no second JSON shape) into `comp_data`-shaped rows (`num`/`parent_num`/`num_cap`/`citem_num` indexing, `entity_idx` path strings built for `COLLATE "C"` ordering, synthetic `_magnitude` fill, type/attribute alias compaction `_type`→`"T"`), and reconstruct losslessly. Golden vectors are in-tree: `crates/ehrbase/tests/resources/rm_db_format/…/*.db_aliased.json`. **Next action:** `rm_db_format/aliases.rs` (static `RmTypeAlias`/`RmAttributeAlias` tables + `LazyLock` reverse maps, ported from the Java lists, uniqueness/round-trip tests), then `structure_rm_type.rs`.

- **App build order (drives this pointer):** P09 ✅ → **P10** rm-db-format → **P11** REST+auth → **P12** service layer (+ minimal parity harness) → **P13** template ingestion (OPT 1.4 XML) → **P14** WebTemplate → **P15** validation → **P16** AQL engine → **P17** FLAT/EhrScape → **P18** integration → **P19** conformance & parity → **P20** optimization → **P99** cutover. Each phase builds **compiling, tested increments**.

- **Discipline for the app phases:** every phase compiles + clippy-clean + tested before moving on; `thiserror` in libs / `anyhow` in the `ehrbase` binary; no `unwrap`/`expect` outside tests; consume `openehr-*` types, never re-model the RM; the `.java` files under `crates/ehrbase*/` are the read-only behavioural reference (deleted per-subsystem at parity, all residue at P99); official CLIs for tool-managed artifacts (`sqlx migrate add`, etc.). Tick the phase checkbox + commit on a `claude/phase-NN-*` branch.

- **Note:** `openehr-term` is deliberately NOT generated (bundle + XML assets + access logic aren't derivable from BMM). Stage 2 (RBAC, plugin system, multi-tenancy) begins only after P19 parity holds.
