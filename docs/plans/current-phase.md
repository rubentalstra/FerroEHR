- Phase: **0G — spec-driven code generation (ADR-004)**. The openEHR spec crates are now GENERATED from the vendored BMM meta-model; hand-transcription is retired. Read `docs/ADRs/ADR-004-spec-driven-codegen.md` and the "Code generation" section of `CLAUDE.md` before touching any `openehr-*` spec crate.
- State: `openehr-base`, `openehr-rm`, `openehr-am` (am14+am24), `openehr-term`, `openehr-lang` are all generated and compile clean (0 warnings, clippy-clean). Regenerate with `cargo run -p openehr-codegen -- emit`. Strong-typing pass done: spurious `serde_json::Value` cut from 100 → 31 (the 31 are all genuine spec `Any` / BMM-omitted element types). Fixes: ancestor generic-bound inheritance; abstract generic enums thread their type param (`Version<T>`); generic-ancestor binding substitution (`Multiplicity_interval`=`Interval<Integer>`, `X_VERSIONED_*` content types); abstract-with-descendants always emitted (`Interval`); nested-`Hash` (`generic_parameter_defs`) parsing; cross-crate LANG refs (`rules`/`includes`); complete Rust-keyword field escaping; untagged-enum recursion boxing; `effective_roots` import precision. **LANG now merges both vendored BMM files** (`openehr_lang_1.1.0.bmm.json` + `-bmm3`) so the persisted-BMM/`EXPR_*`/`STATEMENT_SET` model and the `BMM_*`/`EL_*` model both emit.
- Fidelity gate DONE (`openehr-its/tests/fidelity.rs`), TWO gates: (a) **readability** — the generated `openehr_rm` types read the real openEHR_SDK canonical-JSON corpus (54 read OK, 8 documented exclusions); (b) **lossless round-trip** — deserialize → re-serialize → `semantic_eq` proves no data is silently dropped (**53 lossless, 9 documented exclusions, 0 failed**). `semantic_eq` tolerates only three information-preserving diffs: object key order, fields we omit that input stated empty (`null`/`[]`), and fields we materialize that input left implicit (`_type`, `Interval` `*_included`→true / `*_unbounded`→false defaults). An empirical leaf-diff audit confirmed 52/53 files were already byte-faithful before normalization; the one round-trip-only exclusion is a malformed fixture (`feeder_system_audit` misplaced on a LOCATABLE). Original 8 exclusions: (raw-DB/flat path-keyed compositions ×2, a deliberate invalid fixture, ITS-REST contribution-request shape ×2, one malformed DV_TEXT, and two RM-1.1-era files omitting mandatory LOCATABLE.name/archetype_node_id). Emitter gains this required: **polymorphic-concrete enums** (a concrete class with subtypes — `DV_TEXT`→`DV_CODED_TEXT`, `DV_URI`→`DV_EHR_URI`, `PARTY_IDENTIFIED`→`PARTY_RELATED`, `OBJECT_REF`→`{LOCATABLE_REF,PARTY_REF}` — emits a `{Name}` untagged enum over `{Name}Data` + subtypes, with `enum_variants` returning immediate children so enums nest); **Interval flag defaults** (`#[openehr(default=…)]` in the derive: `*_included`→true, `*_unbounded`→false, since archie omits them); **byte buffers** (`Array<Octet>` like `DV_MULTIMEDIA.data`) → base64 `String`, not `Vec<u8>`.
- Next actions (in priority order):
  1. Externalize the emitter's hardcoded override maps (`type_override`, `class_binding`, `field_default`, `field_type` byte-buffer rule) to `codegen.toml` (seed from `docs/ROSETTA.md`).
  2. Canonical **XML** (ITS-XML) in `openehr-its` via `quick-xml`, validated against the vendored XSDs.
  3. Add a drift-check CI step (regenerate + `git diff --exit-code`).
  4. Stricter fidelity: lossless re-serialize + normalized value-equality + ITS-JSON schema validation (currently proves readability).
- Note: `openehr-term` is deliberately NOT generated (bundle + XML assets + access logic are not derivable from BMM). The `ehrbase-*` application crates are ported from EHRbase Java (not generated).
