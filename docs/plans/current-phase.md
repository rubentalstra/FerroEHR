- **Next phase: P10 — Storage foundation** (`docs/plans/phase-10-rm-db-format.md`, re-scoped by **ADR-008**). Greenfield PG18-native storage: the unified `node` table (nested-set decomposition, canonical JSON fragments), the temporal `vo_version` table, our `ext` helper functions, and the Rust node codec — starting with the storage spike.
- **Read first:** `docs/ADRs/ADR-008-greenfield-pg18-storage.md` (**the pivot** — own storage design; openEHR spec conformance replaces EHRbase parity; EHRbase demoted to prior art and its in-tree Java removed), then ADR-004/005 (the generated spec + ITS foundation, unchanged). ADR-006/007 are partially superseded by ADR-008.

- **Where we are.** Foundation (00–08) done and compiling (generated spec crates, canonical JSON+XML, ITS-REST contract, AQL parser). **P09 done** (2026-07-05): `ehrbase::db` settings/pool/two-schema migrators/testcontainers harness — the *infrastructure* carries forward; the *schema content* (EHRbase baseline, ADR-007) is replaced by our own design at P10. **ADR-008 pivot** (2026-07-05): the mission is the best possible modern-Rust, openEHR-spec-conformant CDR; acceptance = the openEHR CNF conformance schedule; internals are ours (design grounded in the docs-verified PG18 research recorded in ADR-008).

- **The goal.** openEHR-spec-conformant CDR (ITS-REST 1.0.3 + AQL 1.1), modern idiomatic Rust on the generated `openehr-*` crates, PG18-native storage/engine of our own design. Proper crates always (`axum`, `sqlx`+`sea-query`/`sea-query-sqlx`, `oauth2`/`openidconnect`/`jsonwebtoken`/`argon2`, `utoipa`, `moka`, `jiff`); official CLIs for tool-managed artifacts; the bespoke subsystems (node codec, versioning, validation, AQL IR→SQL) are our own designs validated by conformance + corpus tests.

- **App build order:** P09 ✅ → **P10** storage foundation → **P11** REST+auth → **P12** service layer (+ smoke conformance wiring) → **P13** template ingestion → **P14** WebTemplate → **P15** validation → **P16** AQL engine (our IR + BMM-generated RM model) → **P17** FLAT/EhrScape → **P18** integration → **P19** openEHR conformance → **P20** optimization → **P99** cleanup/release. Compiling, tested increments every phase.

- **Session goal (P10).** Run the storage spike first (corpus → candidate schema in testcontainers; CONTAINS/extract/order queries; temporal-PK validation; fragment sizing), record results in the phase file, then author the final migrations (`sqlx migrate add`) + node codec. **Next action:** the spike.

- **Discipline:** every phase compiles + clippy-clean + tested; `thiserror` libs / `anyhow` binary; no `unwrap`/`expect` outside tests; consume `openehr-*` types; never hand-edit `// @generated`; official CLIs; tick checkboxes + commit on `claude/phase-NN-*`. The EHRbase Java is gone from the tree (ADR-008) — consult upstream repos/git history as prior art when useful, and the openEHR specs as the authority.
