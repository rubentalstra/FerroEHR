# A1 Spec Audit — Chapter `its-rest-ehr-composition` (Phase 1: Extract)

- **Date:** 2026-07-11
- **Component:** ITS-REST 1.0.3 EHR API (EHR / EHR_STATUS / VERSIONED_EHR_STATUS / VERSIONED_COMPOSITION / COMPOSITION / DIRECTORY / CONTRIBUTION operations)
- **Oracle:** `docs/specs/openehr/ITS-REST/specifications/` + cross-check against RM `docs/specs/openehr/RM/docs/`

## Spec files read

- `ITS-REST/specifications/ehr.openapi.yaml`
- `operations/`: ehr_create, ehr_create_with_id, ehr_get_by_id, ehr_get_by_subject, ehr_status_update, ehr_status_get_at_time, ehr_status_get_by_version_id, versioned_ehr_status_get, versioned_ehr_status_version_get_by_id, versioned_ehr_status_version_get_at_time, versioned_ehr_status_revision_history, composition_create, composition_get, composition_update, composition_delete, versioned_composition_get, versioned_composition_version_get_by_id, versioned_composition_version_get_at_time, directory_create, directory_update, directory_delete, directory_get_at_time, directory_get_by_version_id, contribution_create, contribution_get
- `responses/`: 201_EHR, 409_EHR, 409_EHR_with_id, 200_EHR, 201_COMPOSITION, 200_COMPOSITION_updated, 200_COMPOSITION_retrieved, 412_COMPOSITION, 422_COMPOSITION, 409_COMPOSITION_with_uid_based_id, 204_COMPOSITION_deleted, 400_COMPOSITION, 400_already_deleted, 200_EHR_STATUS_updated, 204_EHR_STATUS, 412_EHR_STATUS, 201_CONTRIBUTION, 400_CONTRIBUTION, 409, 201_directory, 204_because_deleted, 204_because_deleted_at_time, 400_FOLDER, 412_directory, 406, 400_invalid_version_at_time, 404_unknown_ehr_id_or_no_version_at_time, 404_EHR_subject, 200_VERSION, 200_VERSION_of_COMPOSITION_at_time, 200_VERSIONED_COMPOSITION
- `schemas/`: ehr/EhrStatus, ehr/Composition, ehr/Folder, ehr/Ehr, common/NewContribution, common/UpdateVersion, common/UpdateAudit, common/UpdateAttestation, common/UPartyProxy, common/Versionable, common/UVersion
- `parameters/`: header/If-Match, header/Prefer, query/version_at_time, query/subject_id, path/ehr_id, path/uid_based_id_as_version_uid, path/uid_based_id_as_versioned_object_uid
- `headers/`: ETag_COMPOSITION, Location_COMPOSITION
- RM cross-check: `RM/docs/UML/classes/org.openehr.rm.ehr.ehr_status.adoc`, `RM/docs/ehr/master03-ehr_information_model.adoc`

## Requirements

| id | requirement | citation | category | risk |
|----|-------------|----------|----------|------|
| its-rest-ehr-composition-R1 | `EHR_STATUS.subject` is monomorphic `PARTY_SELF` in the RM (multiplicity 1..1, no subtypes); the write path MUST reject any foreign concrete `_type` (e.g. PARTY_IDENTIFIED / PARTY_RELATED) in the subject slot, even though the ITS-REST `EhrStatus.subject` schema loosely references `UPartyProxy` (oneOf PARTY_SELF/IDENTIFIED/RELATED). KNOWN-FIXED (PR #69 / commit 360c44494): `validate_ehr_status` deserializes subject through generated `PartySelf`. | RM `ehr.ehr_status.adoc` line 21-23 (`subject: PARTY_SELF`, 1..1); ITS-REST `schemas/ehr/EhrStatus.yaml` L14-15 + `schemas/common/UPartyProxy.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R2 | Every OTHER monomorphic RM slot that the ITS-REST wire types loosely (via a `oneOf`/`U…` union) MUST likewise reject a foreign concrete `_type` on its actual write path — the derive `_type` check must run on the concrete target type, not the union. (Open question flagged by chapter notes — verify each write path, not just EHR_STATUS.subject.) | ITS-REST `schemas/common/*.yaml` union schemas vs RM monomorphic attribute types; chapter notes | rejection-duty | high |
| its-rest-ehr-composition-R3 | `EHR_STATUS` requires `subject`, `is_queryable`, `is_modifiable`; a body missing any of the three MUST be rejected (400). `is_queryable`/`is_modifiable` are Boolean 1..1. | `schemas/ehr/EhrStatus.yaml` L5-9 (`required`); RM `ehr.ehr_status.adoc` L21-31 | mandatory-attr | high |
| its-rest-ehr-composition-R4 | `COMPOSITION` requires `language`, `territory`, `category`, `context`, `composer`, `content`; a body missing any MUST be rejected (400). | `schemas/ehr/Composition.yaml` L6-12 | mandatory-attr | high |
| its-rest-ehr-composition-R5 | `COMPOSITION.composer` is a `PARTY_PROXY` slot (`UPartyProxy` oneOf PARTY_SELF/IDENTIFIED/RELATED, discriminated on `_type`); a payload whose `_type` is not one of those three MUST be rejected. | `schemas/ehr/Composition.yaml` L25-26; `schemas/common/UPartyProxy.yaml` L2-11 | rejection-duty | high |
| its-rest-ehr-composition-R6 | `UPDATE_AUDIT.committer` is a `PARTY_PROXY` slot; the committer `_type` MUST be PARTY_SELF/IDENTIFIED/RELATED, else reject. | `schemas/common/UpdateAudit.yaml` L14-16; `schemas/common/UPartyProxy.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R7 | `NewContribution` (contribution_create body) requires `versions` (array of `UPDATE_VERSION`) and `audit` (`UPDATE_AUDIT`); missing either MUST be rejected (400). | `schemas/common/NewContribution.yaml` L4-6 | mandatory-attr | high |
| its-rest-ehr-composition-R8 | `UPDATE_VERSION` requires `lifecycle_state`, `data`, `commit_audit`; missing any MUST be rejected (400). | `schemas/common/UpdateVersion.yaml` L3-6 | mandatory-attr | high |
| its-rest-ehr-composition-R9 | `UPDATE_AUDIT` requires `change_type` (TerminologyCode) and `committer`; missing either MUST be rejected (400). | `schemas/common/UpdateAudit.yaml` L4-6 | mandatory-attr | high |
| its-rest-ehr-composition-R10 | `UPDATE_ATTESTATION` requires `reason` and `is_pending` (in addition to inherited UPDATE_AUDIT requirements); missing either MUST be rejected. | `schemas/common/UpdateAttestation.yaml` L4-8 | mandatory-attr | medium |
| its-rest-ehr-composition-R11 | On `POST /ehr` when the client supplies an EHR_STATUS whose subject id/namespace already belongs to an existing EHR, the server MUST return `409 Conflict` (subject-id+namespace uniqueness). | `operations/ehr_create.yaml` L32; `responses/409_EHR.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R12 | On `PUT /ehr/{ehr_id}` when `ehr_id` is already used by an existing EHR, the server MUST return `409 Conflict`. | `operations/ehr_create_with_id.yaml` L35-36; `responses/409_EHR_with_id.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R13 | On `PUT /ehr/{ehr_id}` the supplied `ehr_id` MUST be a valid HIER_OBJECT_ID value; an invalid value MUST be rejected (400). | `operations/ehr_create_with_id.yaml` L6-7, L33-34 | validity-fn | high |
| its-rest-ehr-composition-R14 | When no EHR_STATUS is supplied on EHR create, the server MUST auto-create a default EHR_STATUS with `is_queryable=true`, `is_modifiable=true`, `subject=PARTY_SELF`. | `operations/ehr_create.yaml` L6-13; `operations/ehr_create_with_id.yaml` L9-16 | behaviour | medium |
| its-rest-ehr-composition-R15 | An EHR_STATUS resource MUST always be created and committed for every new EHR (EHR creation semantics). | `operations/ehr_create.yaml` L7; `operations/ehr_create_with_id.yaml` L9 | behaviour | medium |
| its-rest-ehr-composition-R16 | `POST /ehr/{ehr_id}/composition` request body is REQUIRED; a body that parses but is not a valid COMPOSITION MUST return `400`, and a body that is a valid COMPOSITION but fails semantic/template validation MUST return `422 Unprocessable Entity`. | `operations/composition_create.yaml` L17,21-26; `responses/400_COMPOSITION.yaml`; `responses/422_COMPOSITION.yaml` | status-code | high |
| its-rest-ehr-composition-R17 | `422` (not 400) MUST be returned specifically when the content converts to a COMPOSITION but the underlying template is unknown or does not validate the COMPOSITION. | `responses/422_COMPOSITION.yaml` | status-code | high |
| its-rest-ehr-composition-R18 | On `PUT …/composition/{uid_based_id}` if the request body contains a `COMPOSITION.uid.value` it MUST match the `uid_based_id` in the URL, else reject. | `operations/composition_update.yaml` L8 | rejection-duty | high |
| its-rest-ehr-composition-R19 | `PUT …/composition/{uid_based_id}` `uid_based_id` MUST be a HIER_OBJECT_ID (`versioned_object_uid`) form; and the `If-Match` header (the latest `version_uid` = `preceding_version_uid`) is REQUIRED. | `operations/composition_update.yaml` L6,10,16; `parameters/path/uid_based_id_as_versioned_object_uid.yaml`; `parameters/header/If-Match.yaml` L8 | rejection-duty | high |
| its-rest-ehr-composition-R20 | `PUT …/composition/{uid_based_id}` MUST return `412 Precondition Failed` when the `If-Match` header does not match the latest server-side version, and the response MUST include the latest `version_uid` in both `Location` and `ETag` headers. | `operations/composition_update.yaml` L33-34; `responses/412_COMPOSITION.yaml` | status-code | high |
| its-rest-ehr-composition-R21 | `DELETE …/composition/{uid_based_id}` `uid_based_id` MUST be an OBJECT_VERSION_ID form taken from the latest VERSION.uid.value (`preceding_version_uid`); a value that does not match the latest version MUST return `409`, with latest version echoed in `Location` + `ETag`. | `operations/composition_delete.yaml` L6,20-21; `parameters/path/uid_based_id_as_version_uid.yaml`; `responses/409_COMPOSITION_with_uid_based_id.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R22 | `DELETE …/composition/{uid_based_id}` MUST return `400` when the resource is already deleted (or the request params are unparseable). | `operations/composition_delete.yaml` L16-17; `responses/400_already_deleted.yaml` | status-code | medium |
| its-rest-ehr-composition-R23 | Successful `DELETE …/composition` returns `204 No Content` with `ETag` + `Location` headers of the deleted version (logical delete). | `operations/composition_delete.yaml` L14-15; `responses/204_COMPOSITION_deleted.yaml` | behaviour | medium |
| its-rest-ehr-composition-R24 | `PUT …/ehr_status` request body is REQUIRED; the `If-Match` header (latest `version_uid` = `preceding_version_uid`) is REQUIRED; mismatch MUST return `412` with latest `version_uid` in `Location` + `ETag`. | `operations/ehr_status_update.yaml` L6,13,22; `responses/412_EHR_STATUS.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R25 | EHR_STATUS itself is ALWAYS modifiable — an `is_modifiable=false` flag MUST NOT block updates to the EHR_STATUS object. | RM `ehr.ehr_status.adoc` L9 ("This object is always modifiable"), L30-31 ("The EHR_STATUS object itself can always be written to") | behaviour | high |
| its-rest-ehr-composition-R26 | When the owning EHR's `EHR_STATUS.is_modifiable=false`, writes to the EHR *other than* the EHR_STATUS object (COMPOSITION create/update/delete, DIRECTORY create/update/delete) MUST be blocked/rejected. | RM `ehr.ehr_status.adoc` L30-31 (`is_modifiable`: "True if the EHR, other than the EHR_STATUS object, is allowed to be written to") | rejection-duty | high |
| its-rest-ehr-composition-R27 | On `POST …/contribution`, a supplied CONTRIBUTION `uid` MUST be accepted only if not already in use; if in use, an error (409, resource with same identifier already exists) MUST be returned. | `operations/contribution_create.yaml` L5, L28-29; `responses/409.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R28 | On `POST …/contribution`, `audit.time_committed` MUST always be set by the server (client value ignored/overwritten); `audit.system_id` MUST be validated when provided. | `operations/contribution_create.yaml` L6-7 | behaviour | medium |
| its-rest-ehr-composition-R29 | `POST …/contribution` MUST return `400` when the modification type does not match the operation (e.g. the first version of a composition submitted with change_type MODIFICATION rather than CREATION). | `responses/400_CONTRIBUTION.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R30 | `POST …/directory` request body (FOLDER) is REQUIRED; a body that is not a valid FOLDER MUST return `400`. | `operations/directory_create.yaml` L17,21-22; `responses/400_FOLDER.yaml` | status-code | medium |
| its-rest-ehr-composition-R31 | `PUT …/directory` and `DELETE …/directory` REQUIRE the `If-Match` header (latest `version_uid` = `preceding_version_uid`); mismatch MUST return `412` with latest version in `Location` + `ETag`. | `operations/directory_update.yaml` L6,11,30-31; `operations/directory_delete.yaml` L6,11,19-20; `responses/412_directory.yaml` | rejection-duty | high |
| its-rest-ehr-composition-R32 | Successful `DELETE …/directory` returns `204 No Content` (logical delete). | `operations/directory_delete.yaml` L13-14; `responses/204_because_deleted.yaml` | status-code | low |
| its-rest-ehr-composition-R33 | `GET …/ehr_status` and `GET …/composition/{uid_based_id}` with `version_at_time`: if the value is not valid extended ISO 8601, MUST return `400`. | `operations/ehr_status_get_at_time.yaml` L15-16; `responses/400_invalid_version_at_time.yaml`; `parameters/query/version_at_time.yaml` L3-4 | validity-fn | medium |
| its-rest-ehr-composition-R34 | `GET …/composition/{uid_based_id}` (HIER_OBJECT_ID form): if `version_at_time` supplied, return the version extant at that time; otherwise return the latest version. A version that was deleted at that time MUST return `204`. | `operations/composition_get.yaml` L9,20-22; `responses/204_because_deleted_at_time.yaml` | behaviour | medium |
| its-rest-ehr-composition-R35 | `GET …/ehr_status` and `GET …/versioned_*/version`: absent `version_at_time` returns the LATEST version; present returns the version extant at that time. | `operations/ehr_status_get_at_time.yaml` L4-6; `operations/versioned_composition_version_get_at_time.yaml` L4-6; `operations/versioned_ehr_status_version_get_at_time.yaml` L4-6 | behaviour | medium |
| its-rest-ehr-composition-R36 | `GET /ehr?subject_id=&subject_namespace=` MUST match the query params against `EHR_STATUS.subject.external_ref.id.value` and `EHR_STATUS.subject.external_ref.namespace`; no match MUST return `404`. `subject_id` is a REQUIRED query param. | `operations/ehr_get_by_subject.yaml` L4-6,11-12,16-17; `parameters/query/subject_id.yaml` L5; `responses/404_EHR_subject.yaml` | behaviour | medium |
| its-rest-ehr-composition-R37 | `GET /ehr/{ehr_id}` for an unknown `ehr_id` MUST return `404`. | `operations/ehr_get_by_id.yaml` L12-13; `responses/404_unknown_ehr_id.yaml` | status-code | low |
| its-rest-ehr-composition-R38 | The `If-Match` header format MUST be a `version_uid` identifier enclosed in double quotes; the operation proceeds only if it matches the current `preceding_version_uid`. | `parameters/header/If-Match.yaml` L4-12 | header | high |
| its-rest-ehr-composition-R39 | The `Prefer` header (enum `return=representation` \| `return=minimal`, default `return=minimal`): `return=representation` MUST return the full resource body; otherwise (minimal/missing) only headers are returned (201/200 without body → 204 for EHR_STATUS update). | `parameters/header/Prefer.yaml` L10-13; `responses/201_COMPOSITION.yaml` L2-3; `responses/204_EHR_STATUS.yaml` L1-2 | header | medium |
| its-rest-ehr-composition-R40 | On successful create (201) / update (200) / precondition-fail (412) / delete (204), the `ETag` response header MUST carry the version_uid (double-quoted) and `Location` the resource URL. | `responses/201_EHR.yaml` L4-8; `responses/200_COMPOSITION_updated.yaml` L3-7; `headers/ETag_COMPOSITION.yaml`; `headers/Location_COMPOSITION.yaml` | header | medium |
| its-rest-ehr-composition-R41 | `POST …/composition` and `POST /ehr` for an unknown/absent EHR MUST return `404` (unknown ehr_id). | `operations/composition_create.yaml` L23-24; `responses/404_unknown_ehr_id.yaml` | status-code | medium |
| its-rest-ehr-composition-R42 | A request whose `Accept` header requests a content type/format the service cannot produce MUST return `406 Not Acceptable`. | `responses/406.yaml`; ITS-REST general protocol | status-code | medium |
| its-rest-ehr-composition-R43 | `GET …/versioned_composition/{versioned_object_uid}/version/{version_uid}` and `GET …/versioned_ehr_status/version/{version_uid}` return a `VERSION` (`UVersion` = oneOf ORIGINAL_VERSION / IMPORTED_VERSION, discriminated on `_type`); the serialized version envelope MUST be one of those two. | `operations/versioned_composition_version_get_by_id.yaml` L12-13; `responses/200_VERSION.yaml`; `schemas/common/UVersion.yaml` L2-9 | serialization | medium |
| its-rest-ehr-composition-R44 | `GET …/versioned_composition/{versioned_object_uid}` returns a `VERSIONED_COMPOSITION`; unknown ehr_id or versioned_object_uid MUST return `404`. | `operations/versioned_composition_get.yaml` L11-14; `responses/200_VERSIONED_COMPOSITION.yaml` | status-code | low |
| its-rest-ehr-composition-R45 | `PARTY_PROXY` union serialization MUST dispatch on `_type` per the discriminator mapping (PARTY_SELF→UMPartySelf, PARTY_IDENTIFIED→UMPartyIdentified, PARTY_RELATED→UMPartyRelated); an unmapped `_type` MUST be rejected. | `schemas/common/UPartyProxy.yaml` L6-11 | serialization | medium |
| its-rest-ehr-composition-R46 | `EHR_STATUS` inherits `LOCATABLE` and carries the invariant `Is_archetype_root` (`is_archetype_root` MUST hold) — the EHR_STATUS is an archetype root. | RM `ehr.ehr_status.adoc` L14-15, L37-39 | invariant | low |
| its-rest-ehr-composition-R47 | The EHR resource representation carries `system_id`, `ehr_id`, `ehr_status` (ObjectRef), `ehr_access` (ObjectRef), `time_created`; `ehr_status`/`ehr_access` are references (OBJECT_REF), not embedded objects. | `schemas/ehr/Ehr.yaml` L4-14 | serialization | low |
| its-rest-ehr-composition-R48 | `POST /ehr/{ehr_id}/composition` first-version create: the created resource is the FIRST version of a new COMPOSITION (no preceding version), returning `201` with ETag/Location. | `operations/composition_create.yaml` L4; `responses/201_COMPOSITION.yaml` | behaviour | low |
| its-rest-ehr-composition-R49 | `FOLDER` (directory) inherits `LOCATABLE`; `items` are `OBJECT_REF` entries and `folders` are nested FOLDERs (recursive) — a folder tree, references not embedded content. | `schemas/ehr/Folder.yaml` L4-19 | serialization | low |
| its-rest-ehr-composition-R50 | `GET …/directory` with `path` supplied MUST return only the sub-FOLDER at that path; a non-existent path (or unknown ehr_id / no version at time) MUST return `404` (or `204` when deleted at time). | `operations/directory_get_at_time.yaml` L7,17-20; `operations/directory_get_by_version_id.yaml` L5-6,16-17 | behaviour | low |
| its-rest-ehr-composition-R51 | `GET …/contribution/{contribution_uid}` for an unknown contribution (or ehr) MUST return `404`; a created CONTRIBUTION is returned as a `Contribution` schema on 201 with ETag/Location. | `operations/contribution_get.yaml` L11-14; `responses/201_CONTRIBUTION.yaml` L4-12 | status-code | low |
| its-rest-ehr-composition-R52 | `EHR_STATUS.other_details` is optional and, when present, MUST be an archetyped `ITEM_STRUCTURE`. | `schemas/ehr/EhrStatus.yaml` L22-23; RM `ehr.ehr_status.adoc` L33-35 | mandatory-attr | low |
| its-rest-ehr-composition-R53 | `UPDATE_AUDIT.change_type` and `UPDATE_VERSION.lifecycle_state` are `TERMINOLOGY_CODE` values — MUST validate the code against the corresponding openEHR terminology group (change type / lifecycle state) rather than accepting arbitrary strings. | `schemas/common/UpdateAudit.yaml` L11-12; `schemas/common/UpdateVersion.yaml` L12-13 | validity-fn | medium |
