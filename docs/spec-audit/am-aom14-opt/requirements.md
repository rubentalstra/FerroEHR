# A1 Spec Audit — Phase 1 (Extract) — chapter `am-aom14-opt`

- **Date:** 2026-07-11
- **Component:** AM — AOM 1.4 constraint model + ADL 1.4 cADL constraint semantics + OPT flattening
- **Spec files read (all relative to `docs/specs/openehr/`):**
  - `AM/docs/AOM1.4/master03-archetype_package.adoc`
  - `AM/docs/AOM1.4/master04-constraint_model_package.adoc`
  - `AM/docs/AOM1.4/master05-assertion_package.adoc`
  - `AM/docs/AOM1.4/master06-primitive_package.adoc`
  - Class defs under `AM/docs/UML/classes/org.openehr.am.aom14.*.adoc` (archetype, archetype_constraint, c_attribute, c_single_attribute, c_multiple_attribute, cardinality, c_object, c_defined_object, c_complex_object, c_primitive_object, c_domain_type, archetype_slot, archetype_internal_ref, constraint_ref, c_primitive, c_boolean, c_string, c_integer, c_real, c_date, c_time, c_date_time, c_duration)
  - `AM/docs/ADL1.4/master05-cadl.adoc`
  - `AM/docs/ADL1.4/master06-assertions.adoc`
  - `AM/docs/ADL1.4/master08-adl.adoc` (validity-code table; read for the archetype-level VARxx rules)
  - `AM/docs/UML/classes/org.openehr.am.aom2.operational_template.adoc`

- **File corrections / notes:**
  - **OPT 1.4 has no dedicated spec file in the vendored tree.** There is no AOM1.4 / ADL1.4 `operational_template` document; `OPERATIONAL_TEMPLATE` is defined only under AOM2 (`AM/docs/UML/classes/org.openehr.am.aom2.operational_template.adoc`, dir `AM/docs/OPT2`). The flattening *duties* extracted below (R48–R52) are taken from that AOM2 class description (which is version-neutral about what flattening produces) plus the ADL1.4 specialisation/slot/internal-ref semantics; the ADL 1.4 OPT is a serialization of a fully-flattened archetype and the CNF `I_DEFINITION_ADL14/*_opt` robot fixtures are the executable oracle.
  - The AOM1.4 class definitions are included into `master0[3-6]` via `{uml_export_dir}/classes/...` and physically live under `AM/docs/UML/classes/`; citations point at the physical class file.
  - `master05-primitive` / `master06-primitive` file naming: the primitive *package* overview is `AOM1.4/master06-primitive_package.adoc`; the assertion package is `AOM1.4/master05-assertion_package.adoc` (both read).

Requirements are numbered `am-aom14-opt-R{n}`. Risk: **high** = rejection duty / write-path integrity; **medium** = conformance-visible semantics; **low** = otherwise.

| id | requirement | citation | category | risk |
|----|-------------|----------|----------|------|
| am-aom14-opt-R1 | `C_ATTRIBUTE.existence` invariant `Existence_set`: `existence.lower >= 0 and existence.upper <= 1`. An existence interval with upper > 1 must be rejected as an invalid attribute constraint. | UML/classes/org.openehr.am.aom14.c_attribute.adoc — Invariants `Existence_set` | invariant | high |
| am-aom14-opt-R2 | `C_ATTRIBUTE.rm_attribute_name` invariant `Rm_attribute_name_valid`: `not rm_attribute_name.is_empty`. An attribute constraint with an empty/absent rm attribute name is invalid. | UML/classes/org.openehr.am.aom14.c_attribute.adoc — Invariants `Rm_attribute_name_valid` | invariant | medium |
| am-aom14-opt-R3 | `C_ATTRIBUTE` invariant `Children_validity`: `any_allowed xor children /= Void`. Exactly one of {any_allowed, non-null children} holds — a node that both permits any value and lists child constraints (or neither) is invalid. | UML/classes/org.openehr.am.aom14.c_attribute.adoc — Invariants `Children_validity`; `any_allowed` post `Result := children = Void or else children.is_empty` | invariant | high |
| am-aom14-opt-R4 | `C_SINGLE_ATTRIBUTE` invariant `Members_valid`: every alternative child `co` satisfies `co.occurrences.upper <= 1`. A single-valued attribute whose alternative object constraint has occurrences upper > 1 must be rejected (single attribute can hold at most one value). | UML/classes/org.openehr.am.aom14.c_single_attribute.adoc — Invariants `Members_valid` | rejection-duty | high |
| am-aom14-opt-R5 | `C_MULTIPLE_ATTRIBUTE.cardinality` is mandatory (1..1). A container-attribute constraint (one carrying `cardinality`) must have a CARDINALITY; conversely presence of `cardinality` marks the attribute as container-typed. | UML/classes/org.openehr.am.aom14.c_multiple_attribute.adoc — Attributes `cardinality 1..1`; ADL1.4/master05-cadl.adoc §Cardinality (lines 259, 271) | mandatory-attr | medium |
| am-aom14-opt-R6 | `CARDINALITY.interval`, `is_ordered`, `is_unique` all mandatory (1..1). The cardinality cannot contradict the cardinality of the corresponding RM attribute — a cardinality wider than the RM allows must be rejected. | UML/classes/org.openehr.am.aom14.cardinality.adoc — Attributes + Description ("cannot contradict … reference model") | cardinality | medium |
| am-aom14-opt-R7 | `C_OBJECT.occurrences` upper limit may only be greater than 1 if the owning attribute has cardinality > 1. An object under a single-valued (non-container) attribute with occurrences upper > 1 must be rejected. | UML/classes/org.openehr.am.aom14.c_object.adoc — Attributes `occurrences` ("Upper limit can only be greater than 1 if owning attribute has a cardinality of more than 1") | rejection-duty | high |
| am-aom14-opt-R8 | `C_OBJECT.node_id` is mandatory (1..1); all nodes must have a node_id. For a C_OBJECT under a container `C_ATTRIBUTE` the node_id must be an id-/at-code defined in the archetype terminology; for `C_PRIMITIVE_OBJECT` it takes the special value `Primitive_node_id`. | UML/classes/org.openehr.am.aom14.c_object.adoc — Attributes `node_id` | mandatory-attr | high |
| am-aom14-opt-R9 | `C_DEFINED_OBJECT` invariant `Assumed_value_valid`: `has_assumed_value implies valid_value(assumed_value)`. An assumed value that does not itself satisfy the node's own constraint must be rejected. | UML/classes/org.openehr.am.aom14.c_defined_object.adoc — Invariants `Assumed_value_valid` | invariant | high |
| am-aom14-opt-R10 | Assumed values are definable at the leaf level only (descendants of `C_PRIMITIVE` and `C_DOMAIN_TYPE`); an assumed value on a non-leaf (complex) object is invalid. | AOM1.4/master04-constraint_model_package.adoc §Assumed_value (line 56) | rejection-duty | medium |
| am-aom14-opt-R11 | `C_COMPLEX_OBJECT` invariant `Attributes_valid`: `any_allowed xor (attributes /= Void and not attributes.is_empty)`; `any_allowed` post `Result = attributes.is_empty`. A complex object that is neither any-allowed nor carries a non-empty attributes list is invalid. | UML/classes/org.openehr.am.aom14.c_complex_object.adoc — Invariants `Attributes_valid` | invariant | high |
| am-aom14-opt-R12 | `C_PRIMITIVE_OBJECT` invariant `Item_valid`: `any_allowed xor item /= Void`; `any_allowed` post `Result = item = Void`. A primitive-object node must carry exactly one `item` C_PRIMITIVE constraint unless it is any-allowed. | UML/classes/org.openehr.am.aom14.c_primitive_object.adoc — Invariants `Item_valid` | invariant | high |
| am-aom14-opt-R13 | `ARCHETYPE_SLOT` invariant `Validity`: `any_allowed xor (includes /= Void or excludes /= Void)`. A slot must be either any-allowed or carry at least one of includes/excludes — not both, not neither. | UML/classes/org.openehr.am.aom14.archetype_slot.adoc — Invariants `Validity` | invariant | high |
| am-aom14-opt-R14 | `ARCHETYPE_SLOT` invariants `Includes_valid`/`Excludes_valid`: `includes /= Void implies not includes.is_empty` and likewise for excludes. A present-but-empty includes or excludes list is invalid. | UML/classes/org.openehr.am.aom14.archetype_slot.adoc — Invariants `Includes_valid`, `Excludes_valid` | invariant | medium |
| am-aom14-opt-R15 | An archetype filling a slot must satisfy the `include` assertions and must not satisfy any `exclude` assertion; include/exclude assertions are Perl-regex (or path/property) constraints on the filler archetype's own properties, and their paths refer to the *filling* archetype, not the containing one. | ADL1.4/master05-cadl.adoc §Archetype Slots (lines 539–600); master06-assertions.adoc §Operands (line 106) | rejection-duty | high |
| am-aom14-opt-R16 | Slot filler regex dialect: include/exclude regular expressions on archetype identifiers use PERL regex syntax. A slot with a full archetype identifier and no wildcards predetermines a single allowed filler. | ADL1.4/master05-cadl.adoc §Archetype Slots (lines 563, 585) | behaviour | medium |
| am-aom14-opt-R17 | `ARCHETYPE_INTERNAL_REF` invariant `Consistency`: `not any_allowed`; invariant `Target_path_valid`: `target_path /= Void and then not target_path.is_empty`. An internal ref with any_allowed true, or with an empty target_path, is invalid. | UML/classes/org.openehr.am.aom14.archetype_internal_ref.adoc — Invariants | invariant | high |
| am-aom14-opt-R18 | `ARCHETYPE_INTERNAL_REF` occurrences default rule: when no occurrences is specified for the internal ref, the referenced (target) node's occurrences must be used — NOT the standard `{1..1}` default; a locally specified occurrences overrides. | UML/classes/org.openehr.am.aom14.archetype_internal_ref.adoc — Description; ADL1.4/master05-cadl.adoc §Internal References (lines 515) | behaviour | medium |
| am-aom14-opt-R19 | `use_node` type rule VUNT: the type named in a `use_node`/internal reference must be the same as, or a super-type (per the reference model) of, the RM type of the referenced node. A subtype-narrowing internal ref that names an incompatible type must be rejected. | ADL1.4/master05-cadl.adoc §Internal References — `VUNT` (line 513) | rejection-duty | high |
| am-aom14-opt-R20 | `CONSTRAINT_REF` invariant `Consistency`: `not any_allowed`. A CONSTRAINT_REF (proxy for external/terminology-bound constraints, `[acNNNN]`) may never be any-allowed. | UML/classes/org.openehr.am.aom14.constraint_ref.adoc — Invariants `Consistency` | invariant | medium |
| am-aom14-opt-R21 | `CONSTRAINT_REF.reference` (1..1) must resolve to a constraint code defined in the archetype ontology `constraint_codes`; every `CONSTRAINT_REF.reference` on a C_OBJECT must be found there (`constraint_references_valid`). | UML/classes/org.openehr.am.aom14.constraint_ref.adoc; UML/classes/org.openehr.am.aom14.archetype.adoc — `constraint_references_valid` | rejection-duty | high |
| am-aom14-opt-R22 | `ARCHETYPE` invariant `Inv_concept_valid`: `ontology.has_term_code(concept_code)` — the archetype `concept` must be an at-code defined in the ontology (VARCN). | UML/classes/org.openehr.am.aom14.archetype.adoc — Invariants `Inv_concept_valid`; ADL1.4/master08-adl.adoc `VARCN` (line 547) | invariant | medium |
| am-aom14-opt-R23 | `ARCHETYPE.node_ids_valid`: every node_id found on a C_OBJECT node must be found in `ontology.term_codes`. An archetype with an at-code node_id absent from the terminology must be rejected (VATDF). | UML/classes/org.openehr.am.aom14.archetype.adoc — `node_ids_valid`; ADL1.4/master08-adl.adoc `VATDF` (line 563) | rejection-duty | high |
| am-aom14-opt-R24 | `ARCHETYPE.internal_references_valid`: every `ARCHETYPE_INTERNAL_REF.target_path` must refer to a legitimate node in the archetype definition. An internal ref to a non-existent path must be rejected. | UML/classes/org.openehr.am.aom14.archetype.adoc — `internal_references_valid` | rejection-duty | high |
| am-aom14-opt-R25 | `ARCHETYPE` invariant `Inv_version_validity`: `version /= Void and then version.is_equal(archetype_id.version_id)` — the archetype version must equal the version segment of its archetype id. | UML/classes/org.openehr.am.aom14.archetype.adoc — Invariants `Inv_version_validity` | invariant | medium |
| am-aom14-opt-R26 | `ARCHETYPE` invariant `Inv_specialisation_validity`: `is_specialised implies specialisation_depth > 0`, and `is_specialised implies parent_archetype_id /= Void`. A specialised archetype without a parent id / zero depth is invalid. | UML/classes/org.openehr.am.aom14.archetype.adoc — Invariants + `is_specialised` post | invariant | medium |
| am-aom14-opt-R27 | `ARCHETYPE` structural mandatory attributes: `definition` (C_COMPLEX_OBJECT root, 1..1), `ontology` (1..1), `archetype_id` (1..1), `concept` (1..1); the definition root must be a C_COMPLEX_OBJECT (constraint on a non-primitive object). Missing any is invalid (VARDF/VARON/VARID). | UML/classes/org.openehr.am.aom14.archetype.adoc — Attributes; AOM1.4/master03-archetype_package.adoc (line 5); ADL1.4/master08-adl.adoc VARDF/VARON/VARID | mandatory-attr | high |
| am-aom14-opt-R28 | VARDT: the topmost type name in the archetype `definition` must match the RM type named in the type slot of the first segment of the archetype id. A mismatch must be rejected. | ADL1.4/master08-adl.adoc `VARDT` (line 556) | rejection-duty | high |
| am-aom14-opt-R29 | VARID: the archetype must have an archetype_id that conforms to the published openEHR archetype-identifier spec; an ill-formed identifier must be rejected. | ADL1.4/master08-adl.adoc `VARID` (line 544) | rejection-duty | high |
| am-aom14-opt-R30 | VDFAI: any archetype identifier mentioned in an archetype slot in the definition must conform to the archetype-identifier spec. | ADL1.4/master08-adl.adoc `VDFAI` (line 573) | rejection-duty | medium |
| am-aom14-opt-R31 | VDFPT: any path mentioned in the definition section must be syntactically valid and a valid path w.r.t. the hierarchical structure of the definition. Invalid paths must be rejected. | ADL1.4/master08-adl.adoc `VDFPT` (line 576) | rejection-duty | medium |
| am-aom14-opt-R32 | VACDF: each constraint code (`acNNNN`) used in the definition must be defined in the `constraint_definitions` part of the ontology. | ADL1.4/master08-adl.adoc `VACDF` (line 566) | rejection-duty | medium |
| am-aom14-opt-R33 | Existence constraint permitted values are exactly `{0}`, `{0..0}`, `{0..1}`, `{1}`, `{1..1}`; the default when none is stated is `{1..1}`. `{0}`/`{0..0}` means the attribute must not be present. | ADL1.4/master05-cadl.adoc §Existence (line 210) | validity-fn | medium |
| am-aom14-opt-R34 | Occurrences default is `{1..1}` when none stated; `{0..0}` / `{0}` means no occurrences of this object are allowed. Occurrences applies to object nodes only (not attribute nodes). | ADL1.4/master05-cadl.adoc §Occurrences (lines 316, 284) | validity-fn | medium |
| am-aom14-opt-R35 | VCOC (cardinality/occurrences validity): the interval `(sum of all child occurrences minima) .. (sum of all child occurrences maxima)` must lie inside the container attribute's cardinality interval. A slot set whose summed occurrences exceed cardinality must be rejected. | ADL1.4/master05-cadl.adoc §Occurrences — `VCOC` (line 324) | rejection-duty | high |
| am-aom14-opt-R36 | A cADL constraint may only *further constrain* the reference model — it cannot be weaker/stronger than the model (e.g. cannot make an RM-mandatory attribute optional). A constraint that widens beyond the RM must be rejected. | ADL1.4/master05-cadl.adoc §Complex Objects (line 173) | rejection-duty | high |
| am-aom14-opt-R37 | Under a single-valued (no-cardinality) attribute, multiple object blocks of the same class are **alternatives**: runtime data need match only one. Validation must accept a value matching any one alternative, not require all. | ADL1.4/master05-cadl.adoc §Single-valued Attributes (lines 234–237) | behaviour | medium |
| am-aom14-opt-R38 | A type-constraint node (e.g. `value matches { QUANTITY matches {*} }`) requires the runtime object to be of the named RM type (or an allowed subtype); a foreign `_type` must be rejected. For a monomorphic slot (RM type with no subtypes) any other `_type` is invalid. | ADL1.4/master05-cadl.adoc §"Any" Constraints (lines 351); UML/classes/org.openehr.am.aom14.c_object.adoc `rm_type_name` | rejection-duty | high |
| am-aom14-opt-R39 | `C_STRING`: string constraints are **case-sensitive**. `valid_value` matches against `list` (fixed strings) and/or `pattern` (regex). `list_open` true means the list is non-exhaustive; when list_open is false, a value outside the list must be rejected. | UML/classes/org.openehr.am.aom14.c_string.adoc; ADL1.4/master05-cadl.adoc §Constraints on String (line 670) | validity-fn | high |
| am-aom14-opt-R40 | `C_STRING` regex dialect is a proper **subset of Perl** regex; `/.../` and `=~ /.../` mean "must match", `!~ /.../` means "must NOT match". A value failing a `/pat/` (or matching a `!~` pattern) must be rejected. Only the documented atomic/grouping/occurrence/class/alternation elements are supported. | ADL1.4/master05-cadl.adoc §Regular Expression (lines 687–739) | validity-fn | high |
| am-aom14-opt-R41 | `C_BOOLEAN`: `true_valid` and `false_valid` cannot both be False (that would make the boolean unsatisfiable). A C_BOOLEAN with both false must be rejected. | UML/classes/org.openehr.am.aom14.c_boolean.adoc — Description | rejection-duty | medium |
| am-aom14-opt-R42 | `C_INTEGER` / `C_REAL`: value constrained by `list` (enumerated values) and/or `range` (Interval). A value outside the list (when set) and outside the range must be rejected. Real literals require a decimal point + ≥1 digit. | UML/classes/org.openehr.am.aom14.c_integer.adoc, c_real.adoc; ADL1.4/master05-cadl.adoc §Integer/§Real (lines 743–800) | validity-fn | high |
| am-aom14-opt-R43 | `C_DATE`/`C_DATE_TIME`/`C_TIME` field-validity monotonicity invariants: if a field is `optional` then each field to its right must be `optional` or `disallowed`; if a field is `disallowed` then each field to its right must be `disallowed` (Month→Day→Hour→Minute→Second→Millisecond chain). A pattern violating this ordering must be rejected. | UML/classes/org.openehr.am.aom14.c_date.adoc, c_date_time.adoc, c_time.adoc — `*_validity_optional`/`*_validity_disallowed` invariants; ADL1.4/master05-cadl.adoc lines 858–861 | invariant | high |
| am-aom14-opt-R44 | Date/time constraint patterns: only the enumerated legal patterns are valid (`yyyy-mm-dd`, `yyyy-mm-??`, `yyyy-??-??`, `yyyy-mm-XX`, `yyyy-??-XX`, time/date-time equivalents). `Pattern_validity` invariant: a non-conformant pattern (`valid_iso8601_*_constraint_pattern`) must be rejected. `??`=optional field, `XX`=field not allowed. | UML/classes/org.openehr.am.aom14.c_date.adoc etc — `Pattern_validity`; ADL1.4/master05-cadl.adoc lines 863–892 | validity-fn | high |
| am-aom14-opt-R45 | Timezone constraint: a timezone modifier can be *required* by appending `±hh`/`±hh:mm`/`±hhmm`/`Z`; absence of a timezone constraint means timezone is optional. There is **no way to prohibit** a timezone — a constraint attempting to prohibit tz is not expressible/should not be manufactured. | ADL1.4/master05-cadl.adoc §Patterns (lines 852–854, 896–910) | behaviour | low |
| am-aom14-opt-R46 | `C_DATE`/`C_DATE_TIME`/`C_TIME` invariant `Validity_is_range`: `validity_is_range = (range /= Void)` — the range form and the field-validity/pattern form are mutually exclusive views; when a `range` is present the constraint is a range constraint. | UML/classes/org.openehr.am.aom14.c_date.adoc, c_date_time.adoc, c_time.adoc — `Validity_is_range` | invariant | low |
| am-aom14-opt-R47 | `C_DURATION`: per-designator allowed flags (`years_allowed`…`fractional_seconds_allowed`) plus optional `range`. Allowed patterns are `P[Y][M][D][T[H][M][S]]` and `P[W]` (openEHR permits mixing `W` with others); interval values may be negated. A duration value using a disallowed designator must be rejected. | UML/classes/org.openehr.am.aom14.c_duration.adoc — Description + Attributes; ADL1.4/master05-cadl.adoc §Duration Constraints (lines 934–980) | validity-fn | medium |
| am-aom14-opt-R48 | An OPERATIONAL_TEMPLATE is a fully **flattened** artefact derived from a TEMPLATE plus all referenced ARCHETYPEs / TEMPLATE_OVERLAYs; validating and generating RM-canonical instance data must be done against the flattened OPT, not the raw archetypes. | UML/classes/org.openehr.am.aom2.operational_template.adoc — Description (lines 9–11) | behaviour | high |
| am-aom14-opt-R49 | OPERATIONAL_TEMPLATE invariant `Specialisation_validity`: `is_specialised` must be true — an OPT is always the specialisation product of the template chain (it is never a bare top-level archetype). | UML/classes/org.openehr.am.aom2.operational_template.adoc — Invariants `Specialisation_validity` | invariant | medium |
| am-aom14-opt-R50 | OPT flattening must carry `component_terminologies` (flattened terminologies of every referenced archetype, keyed by archetype id) — "almost always present"; terminology lookups for node_ids/ac-codes during composition validation resolve against these flattened terminologies. | UML/classes/org.openehr.am.aom2.operational_template.adoc — Attributes `component_terminologies`, `terminology_extracts` | behaviour | medium |
| am-aom14-opt-R51 | OPT flattening must expand `ARCHETYPE_INTERNAL_REF` targets and fill `ARCHETYPE_SLOT`s with their bound archetypes (per specialisation/slot semantics) so the flattened tree is a self-contained constraint structure; slot occurrences and internal-ref occurrences resolve per R18/R35 before flattening. | UML/classes/org.openehr.am.aom2.operational_template.adoc — Description ("process of flattening"); ADL1.4/master05-cadl.adoc §Internal References, §Archetype Slots | behaviour | medium |
| am-aom14-opt-R52 | A specialised (child) archetype constraint may only **narrow** the parent's constraint (`is_subset_of` semantics on ARCHETYPE_CONSTRAINT): a specialised node whose constraint is not narrower-or-equal to the corresponding parent node must be rejected during flatten/validation. | UML/classes/org.openehr.am.aom14.archetype_constraint.adoc — `is_subset_of`; ADL1.4/master05-cadl.adoc §Complex Objects (line 173) | rejection-duty | high |
| am-aom14-opt-R53 | `C_DOMAIN_TYPE` (e.g. `C_DVQUANTITY`, `C_CODE_PHRASE`) must have a `standard_equivalent` (C_COMPLEX_OBJECT form); a domain-typed leaf's custom semantics validate the RM object per its own `valid_value`, and where reduced to standard form must produce an equivalent C_COMPLEX_OBJECT constraint. | UML/classes/org.openehr.am.aom14.c_domain_type.adoc — `standard_equivalent`; AOM1.4/master04-constraint_model_package.adoc §Domain-specific Extensions (lines 76–79) | behaviour | low |
| am-aom14-opt-R54 | Archetype invariants (the `invariants: List<ASSERTION>` on ARCHETYPE / C_COMPLEX_OBJECT) are FOL assertions over ≥2 attributes (e.g. `systolic >= diastolic`); a composition must satisfy every such invariant of its operational template. `Inv_invariants_valid`: if present the list must be non-empty. | UML/classes/org.openehr.am.aom14.archetype.adoc — `invariants`, `Inv_invariants_valid`; AOM1.4/master04-constraint_model_package.adoc §Assertions (line 95); master06-assertions.adoc | behaviour | medium |
| am-aom14-opt-R55 | `ARCHETYPE_CONSTRAINT` invariant `Path_exists`: `path /= Void` for every node; the `is_valid` function is recursive — a node is valid only if the node and all sub-nodes are internally valid. Validation must descend the whole constraint tree, not stop at the root. | UML/classes/org.openehr.am.aom14.archetype_constraint.adoc — Invariants + `is_valid`; AOM1.4/master04-constraint_model_package.adoc §All Node Types (line 19) | invariant | medium |
| am-aom14-opt-R56 | `C_DEFINED_OBJECT.valid_value(a_value)` is the runtime data-conformance function and is designed for recursive cascade from the archetype root; `default_value`/`prototype_value` generate prototype objects. Composition validation must implement `valid_value` as a top-down cascade over the flattened OPT. | UML/classes/org.openehr.am.aom14.c_defined_object.adoc — `valid_value`, `prototype_value`; AOM1.4/master04-constraint_model_package.adoc §Valid_value (line 62) | behaviour | medium |
</content>
