# A1 Spec Audit — Phase 1 (Extract) — chapter `rm-common-change-control`

- **Date:** 2026-07-11
- **Component:** openEHR RM 1.2.0, `common` — change control + generic + archetyped (LOCATABLE)
- **Spec files read (all under `docs/specs/openehr/`):**
  - `RM/docs/common/master06-change_control_package.adoc` (VERSIONED_OBJECT, VERSION, ORIGINAL_VERSION, IMPORTED_VERSION, CONTRIBUTION, REVISION_HISTORY semantics)
  - `RM/docs/common/master04-generic_package.adoc` (AUDIT_DETAILS, ATTESTATION, PARTICIPATION, PARTY_PROXY family, REVISION_HISTORY)
  - `RM/docs/common/master03-archetyped_package.adoc` (LOCATABLE / PATHABLE)
  - `RM/docs/UML/classes/org.openehr.rm.common.versioned_object.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.version.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.original_version.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.imported_version.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.contribution.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.audit_details.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.attestation.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.participation.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.party_proxy.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.party_identified.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.party_related.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.party_self.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.revision_history.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.revision_history_item.adoc`
  - `RM/docs/UML/classes/org.openehr.rm.common.locatable.adoc`
- **Focus (per chapter note):** the per-attribute invariants and write-path rejection duties that the PR #33 1:1 change-control audit could not cover — OBJECT_VERSION_ID wire structure, preceding_version_uid rules, lifecycle-state transition legality, attestation/audit mandatory attributes.

All requirements are machine-checkable. `risk = high` for rejection duties / write-path integrity, `medium` for conformance-visible semantics, `low` otherwise.

| ID | Requirement | Citation | Category | Risk |
|---|---|---|---|---|
| rm-common-change-control-R1 | VERSION invariant `Preceding_version_uid_validity`: `uid.version_tree_id.is_first xor preceding_version_uid /= Void` — a first (trunk root) version MUST have Void `preceding_version_uid`; every non-first version MUST carry a `preceding_version_uid`. A commit violating either arm must be rejected. | version.adoc, Invariants "Preceding_version_uid_validity" (L69) | invariant | high |
| rm-common-change-control-R2 | `VERSIONED_OBJECT.commit_original_version` precondition `all_version_ids.has(a_preceding_version_uid) or else version_count = 0`: a preceding-version id must reference an existing version in this container, unless this is the first version (empty container). Reject a preceding id that is unknown, a preceding id supplied on an empty container, or a missing preceding on a non-empty container. | versioned_object.adoc, commit_original_version Pre (L108) | rejection-duty | high |
| rm-common-change-control-R3 | A VERSION `uid` is an `OBJECT_VERSION_ID`: a three-part identifier `{object_id, creating_system_id, version_tree_id}`, in which `object_id()` is a copy of the owning `VERSIONED_OBJECT.uid`. On the wire every version uid must follow `"<object_id>::<creating_system_id>::<version_tree_id>"`; reject a version uid whose `object_id()` differs from its container uid. | master06 §Version Identification (L203-205); version.adoc uid function (L34) | serialization | high |
| rm-common-change-control-R4 | VERSION invariant `Owner_id_valid`: `owner_id.value.is_equal(uid.object_id.value)` — the derived owner id equals the `object_id` part of the version uid. | version.adoc, Invariants "Owner_id_valid" (L66) | invariant | medium |
| rm-common-change-control-R5 | `version_tree_id` trunk numbering starts at 1 and continues by single increments; branch ids add a pair (branch-number.version, each starting at 1), giving forms like `1.1.1`, `2.3.3`. Trunk version ids are single integers. | master06 §Local Versioning (L219-226) | validity-fn | medium |
| rm-common-change-control-R6 | On a new locally-created ORIGINAL_VERSION, `uid.creating_system_id` is set to the local system id; on an imported version `creating_system_id` retains the originating system's id (never overwritten locally). | master06 §Distributed Versioning (L236-238) | behaviour | medium |
| rm-common-change-control-R7 | Local modifications to a version copied from another system MUST use branching version ids (not new trunk ids), so remote changes are globally distinguishable from originating-system trunk versions. | master06 §Distributed Versioning (L240); §Subsequent Local Modifications (L263) | behaviour | medium |
| rm-common-change-control-R8 | VERSION invariant `Lifecycle_state_valid`: `lifecycle_state` is non-Void and its defining code is in the openEHR terminology `version lifecycle state` group — the only valid codes are `532|complete|`, `553|incomplete|`, `523|deleted|`, `800|inactive|`, `801|abandoned|`. Reject any other code. | version.adoc, Invariants "Lifecycle_state_valid" (L72); master06 §Version Lifecycle (L131) | rejection-duty | high |
| rm-common-change-control-R9 | Any lifecycle-state transition requires the commit of a NEW version, even if content is otherwise unchanged; a sequence of state transitions entails one commit per transition (no in-place mutation of lifecycle_state). | master06 §Version Lifecycle (L139) | behaviour | medium |
| rm-common-change-control-R10 | The `incomplete`→`complete` transition always generates a new VERSION, even when the incomplete content was already technically valid and no new content is added. | master06 §Incomplete Content (L151) | behaviour | medium |
| rm-common-change-control-R11 | Data committed in the `complete` lifecycle state must satisfy full archetype/template validity; content that is technically incomplete (missing mandatory fields) committed as `complete` must be treated as invalid and rejected by the API. | master06 §Incomplete Content (L145-147) | rejection-duty | high |
| rm-common-change-control-R12 | In the `incomplete` state a bounded relaxation applies: single-valued mandatory attributes may be null and container attributes may be empty (existence/cardinality lower limits treated as zero); ALL other validity requirements must still hold ("data may be missing, but it may not be wrong"). | master06 §Incomplete Content NOTE (L147-149) | validity-fn | medium |
| rm-common-change-control-R13 | Logical deletion procedure: create a new version, set its `data` to Void, set `lifecycle_state` to code `523|deleted|`, and commit normally. Deletion never physically removes prior content. | master06 §Logical Deletion (L192-199); §Contributions (L61) | behaviour | medium |
| rm-common-change-control-R14 | `AUDIT_DETAILS.change_type` codes per logical change: addition→`249|creation|`; deletion→`523|deleted|`; correction→`250|amendment|`; content modification→`251|modification|`; import→`249|creation|`; attestation→`666|attestation|`. | master06 §Contributions (L60-66) | behaviour | medium |
| rm-common-change-control-R15 | CONTRIBUTION committal is atomic (nested-transaction semantics): the commit succeeds only if every Version and/or Attestation in the Contribution commits successfully; otherwise the whole Contribution fails. | master06 §Committal and Audits (L92) | behaviour | high |
| rm-common-change-control-R16 | The `system_id`, `committer`, and `time_committed` of the CONTRIBUTION's audit are copied into the corresponding attributes of the `commit_audit` of every VERSION included in the Contribution. | master06 §Committal and Audits (L88) | behaviour | medium |
| rm-common-change-control-R17 | `time_committed` (in both CONTRIBUTION and VERSION audits) must reflect the server-side time of committal (availability to other users) and must therefore be computed on the server, not taken from a client-supplied value. | master06 §Committal and Audits (L90) | behaviour | medium |
| rm-common-change-control-R18 | `VERSIONED_OBJECT.commit_attestation` precondition `has_version_id(a_ver_id) and is_original_version(a_ver_id)`: attestations may only be added to an ORIGINAL_VERSION that exists in the container. Reject an attestation targeting an unknown version id or a non-ORIGINAL (e.g. IMPORTED) version. | versioned_object.adoc, commit_attestation Pre (L141-143); master06 §Contributions (L66) | rejection-duty | high |
| rm-common-change-control-R19 | A merged ORIGINAL_VERSION records the `OBJECT_VERSION_ID` of every source version considered in the merge (other than the preceding version) in `other_input_version_uids`, even for sources whose content was not used; the preceding version is NOT included (it is already in `preceding_version_uid`). | master06 §Version Merging (L296) | behaviour | medium |
| rm-common-change-control-R20 | `IMPORTED_VERSION.item` is a monomorphic slot typed exactly to `ORIGINAL_VERSION` (1..1). Reject an imported version whose `item` payload is not an ORIGINAL_VERSION (no `IMPORTED_VERSION` nesting, no foreign `_type`). | imported_version.adoc, item attribute (L18-20) | rejection-duty | high |
| rm-common-change-control-R21 | IMPORTED_VERSION `uid`, `preceding_version_uid`, `lifecycle_state`, and `data` are computed (effected) from the wrapped item: `uid = item.uid`, `preceding_version_uid = item.preceding_version_uid`, `lifecycle_state = item.lifecycle_state`, `data = item.data`. An imported version has no independent version identifier. | imported_version.adoc, effected functions (L26-49); master06 §Version and its Subtypes (L39) | behaviour | medium |
| rm-common-change-control-R22 | IMPORTED_VERSION carries its OWN `contribution` and `commit_audit` (inherited from VERSION) recording the LOCAL act of committal, distinct from the contribution/audit of the wrapped ORIGINAL_VERSION which record the original committal. | imported_version.adoc Description (L8-9); master06 §Committal and Audits (L84) | mandatory-attr | medium |
| rm-common-change-control-R23 | A VERSION digital signature/`canonical_form` is produced by serialising all attributes EXCEPT `signature` (it is Void at signing time); the signature/digest is then written back into the `signature` attribute. | master06 §Digital Signature (L96,L104); version.adoc canonical_form (L52-53) | behaviour | medium |
| rm-common-change-control-R24 | VERSIONED_OBJECT invariant `Uid_validity`: `uid.extension.is_empty` — the container `uid` is a `HIER_OBJECT_ID` with NO extension (bare GUID). Reject a versioned-object uid carrying an extension. | versioned_object.adoc, Invariants "Uid_validity" (L158) | rejection-duty | high |
| rm-common-change-control-R25 | VERSIONED_OBJECT mandatory attributes: `uid` (HIER_OBJECT_ID, 1..1), `owner_id` (OBJECT_REF, 1..1), `time_created` (DV_DATE_TIME, 1..1). | versioned_object.adoc, Attributes (L15-25) | mandatory-attr | medium |
| rm-common-change-control-R26 | VERSION mandatory attributes: `contribution` (OBJECT_REF, 1..1) and `commit_audit` (AUDIT_DETAILS, 1..1); `signature` is optional (0..1, String). | version.adoc, Attributes (L15-25) | mandatory-attr | medium |
| rm-common-change-control-R27 | AUDIT_DETAILS invariant `System_id_valid`: `not system_id.is_empty` — reject a commit whose audit `system_id` is empty. | audit_details.adoc, Invariants "System_id_valid" (L36) | rejection-duty | high |
| rm-common-change-control-R28 | AUDIT_DETAILS invariant `Change_type_valid`: `change_type.defining_code` must be in the openEHR terminology `audit change type` group. Reject an audit whose change_type code is not in that group. | audit_details.adoc, Invariants "Change_type_valid" (L39) | rejection-duty | high |
| rm-common-change-control-R29 | AUDIT_DETAILS mandatory attributes: `system_id` (String, 1..1), `time_committed` (DV_DATE_TIME, 1..1), `change_type` (DV_CODED_TEXT, 1..1), `committer` (PARTY_PROXY, 1..1); `description` optional (DV_TEXT, 0..1). | audit_details.adoc, Attributes (L15-33) | mandatory-attr | medium |
| rm-common-change-control-R30 | ATTESTATION mandatory attributes (beyond inherited AUDIT_DETAILS): `reason` (DV_TEXT, 1..1) and `is_pending` (Boolean, 1..1); `attested_view`, `proof`, `items` are optional. | attestation.adoc, Attributes (L22-41) | mandatory-attr | medium |
| rm-common-change-control-R31 | ATTESTATION invariant `Reason_valid`: if `reason.generating_type` is `DV_CODED_TEXT`, its defining code must be in the openEHR terminology `attestation reason` group. | attestation.adoc, Invariants "Reason_valid" (L46) | rejection-duty | medium |
| rm-common-change-control-R32 | ATTESTATION invariant `Items_valid`: `items /= Void implies not items.is_empty` — an attestation must not carry a present-but-empty items list. | attestation.adoc, Invariants "Items_valid" (L43) | invariant | medium |
| rm-common-change-control-R33 | ATTESTATION `items`, when present, is a `List<DV_EHR_URI>` of fully-qualified runtime paths; an empty/absent list means the attestation applies to the entire VERSION. | attestation.adoc, items attribute (L30-32); master04 §Attestation (L67) | serialization | low |
| rm-common-change-control-R34 | PARTY_IDENTIFIED invariant `Basic_validity`: `name /= Void or identifiers /= Void or external_ref /= Void` — at least one of name, identifiers, or external_ref must be present. Reject a PARTY_IDENTIFIED with none of the three. | party_identified.adoc, Invariants "Basic_validity" (L31) | rejection-duty | high |
| rm-common-change-control-R35 | PARTY_IDENTIFIED invariant `Name_valid`: `name /= Void implies not name.is_empty` — reject a present-but-empty name. | party_identified.adoc, Invariants "Name_valid" (L34) | invariant | medium |
| rm-common-change-control-R36 | PARTY_IDENTIFIED invariant `Identifiers_valid`: `identifiers /= Void implies not identifiers.is_empty` — reject a present-but-empty identifiers list. | party_identified.adoc, Invariants "Identifiers_valid" (L37) | invariant | medium |
| rm-common-change-control-R37 | PARTY_RELATED: `relationship` (DV_CODED_TEXT, 1..1) is mandatory and its defining code must be in the openEHR terminology `subject relationship` group (invariant `Relationship_valid`). Reject a PARTY_RELATED missing relationship or with an out-of-group code. | party_related.adoc, relationship attr (L18-20) + Invariants "Relationship_valid" (L23) | rejection-duty | high |
| rm-common-change-control-R38 | `AUDIT_DETAILS.committer` and `PARTICIPATION.performer` are `PARTY_PROXY` — concretely `PARTY_SELF` (subject of record) or `PARTY_IDENTIFIED`/`PARTY_RELATED`; `PARTY_PROXY.external_ref` is optional (PARTY_REF, 0..1). | party_proxy.adoc (L15-17); master04 §Audit Details (L49) | mandatory-attr | low |
| rm-common-change-control-R39 | PARTICIPATION mandatory attributes: `function` (DV_TEXT, 1..1) and `performer` (PARTY_PROXY, 1..1); `mode` and `time` optional. | participation.adoc, Attributes (L17-31) | mandatory-attr | low |
| rm-common-change-control-R40 | PARTICIPATION invariant `Function_valid`: if `function.generating_type` is `DV_CODED_TEXT`, its defining code must be in the openEHR terminology `participation function` group. | participation.adoc, Invariants "Function_valid" (L34) | rejection-duty | medium |
| rm-common-change-control-R41 | PARTICIPATION invariant `Mode_valid`: `mode /= Void implies` its defining code is in the openEHR terminology `participation mode` group. | participation.adoc, Invariants "Mode_valid" (L38) | invariant | low |
| rm-common-change-control-R42 | REVISION_HISTORY_ITEM invariant `Audit_valid`: `not audits.is_empty` — there is always at least one commit audit (which may itself be an ATTESTATION). | revision_history_item.adoc, Invariants "Audit_valid" (L24) | invariant | medium |
| rm-common-change-control-R43 | REVISION_HISTORY_ITEM mandatory attributes: `version_id` (OBJECT_VERSION_ID, 1..1) and `audits` (List<AUDIT_DETAILS>, 1..1). | revision_history_item.adoc, Attributes (L15-21) | mandatory-attr | low |
| rm-common-change-control-R44 | REVISION_HISTORY `items` is ordered most-recent-last: `most_recent_version` post `Result = items.last.version_id.value`, `most_recent_version_time_committed` post `Result = items.last.audits.first.time_committed.value`. | revision_history.adoc, functions (L23-33) | validity-fn | low |
| rm-common-change-control-R45 | CONTRIBUTION mandatory attributes: `uid` (HIER_OBJECT_ID, 1..1), `versions` (List<OBJECT_REF>, 1..1), `audit` (AUDIT_DETAILS, 1..1). | contribution.adoc, Attributes (L15-25) | mandatory-attr | medium |
| rm-common-change-control-R46 | LOCATABLE invariant `Archetyped_valid`: `is_archetype_root xor archetype_details = Void` — a top-level versioned object (COMPOSITION/FOLDER/EHR_STATUS root) is an archetype root and MUST carry `archetype_details`; non-root nodes must NOT. | locatable.adoc, Invariants "Archetyped_valid" (L60) | invariant | medium |
| rm-common-change-control-R47 | LOCATABLE invariant `Archetype_node_id_valid`: `not archetype_node_id.is_empty` — reject a locatable node with empty `archetype_node_id`. | locatable.adoc, Invariants "Archetype_node_id_valid" (L63) | rejection-duty | medium |
| rm-common-change-control-R48 | LOCATABLE invariant `Links_valid`: `links /= Void implies not links.is_empty` — reject a present-but-empty links list. | locatable.adoc, Invariants "Links_valid" (L57) | invariant | low |
| rm-common-change-control-R49 | Top-level versioned content (COMPOSITION, EHR_STATUS, etc.) SHOULD set its own `LOCATABLE.uid` to a copy of the owning VERSION's `uid.object_id()` (the leading GUID). | master03 §Unique Node Identification (L41) | behaviour | low |
| rm-common-change-control-R50 | ORIGINAL_VERSION invariant `Is_merged_validity`: `other_input_version_ids = Void xor is_merged` — `is_merged()` is True iff `other_input_version_uids` is present (created from more than just the preceding version). | original_version.adoc, Invariants "Is_merged_validity" (L54) | invariant | medium |
| rm-common-change-control-R51 | ORIGINAL_VERSION invariant `Attestations_valid`: `attestations /= Void implies not attestations.is_empty` — reject a present-but-empty attestations list. | original_version.adoc, Invariants "Attestations_valid" (L51) | invariant | medium |
| rm-common-change-control-R52 | ORIGINAL_VERSION invariant `Other_input_version_uids_valid`: `other_input_version_uids /= Void implies not other_input_version_uids.is_empty` — reject a present-but-empty other-input list. | original_version.adoc, Invariants "Other_input_version_uids_valid" (L57) | invariant | low |
| rm-common-change-control-R53 | ORIGINAL_VERSION attribute typing: `uid` (OBJECT_VERSION_ID, 1..1) and `lifecycle_state` (DV_CODED_TEXT, 1..1) are mandatory; `preceding_version_uid`, `other_input_version_uids`, `attestations`, and `data` are optional (0..1) — a deleted version legitimately has Void `data`. | original_version.adoc, Attributes (L18-40) | mandatory-attr | medium |
| rm-common-change-control-R54 | On import: if no VERSIONED_OBJECT exists for the received `ORIGINAL_VERSION.uid.object_id()`, create one whose uid equals that object_id (logical clone); when creating the enclosing EHR for a first-ever copy, reuse the source EHR identifier. | master06 §Copying (L255-257) | behaviour | medium |
| rm-common-change-control-R55 | Before creating a new VERSIONED_OBJECT with a given uid, verify no container with the same uid already exists (dedupe) — no system creates a duplicate version tree for an existing uid. | master06 §Subsequent Local Modifications (L276) | behaviour | medium |
| rm-common-change-control-R56 | VERSIONED_OBJECT count invariants: `version_count >= 0`, `all_version_ids.count = version_count`, `all_versions.count = version_count`, and `version_count > 0 implies latest_version /= Void`. | versioned_object.adoc, Invariants (L146-155) | invariant | low |
